# 软考复习总笔记（按天累积）

> **用途**：学生视角的一页纸复习卡，每课结束后由老师自动追加当天内容。
> **格式**：每个考点 = 要点 + 固定算例 + 易错点 + 口诀。
> **用法**：忘了随时翻对应 Day；考前串讲（Day 48~49）直接以此为底稿。

---

## Day 1 ｜ 计算机组成（一）：CPU 与寄存器

### CPU 组成
- **要点**：CPU = 运算器 + 控制器 + 寄存器组；主存**不属于** CPU
- **算例**：问"不属于 CPU 的是"→ 主存储器
- **易错**：内存/Cache 混进 CPU 选项
- **口诀**：算盘指挥（算=运算器，盘=寄存器组，指挥=控制器）

### 运算器四件套
- **要点**：ALU 算术逻辑运算；AC 累加器存中间结果；PSW 记进位/溢出状态；通用寄存器组暂存操作数
- **易错**："完成算术与逻辑运算"→ ALU；"暂存中间结果"→ AC

### 控制器四件套
- **要点**：PC 存**下一条**指令地址；IR 存**当前执行**的指令；指令译码器翻译操作码；时序发生器发节拍
- **算例**："PC 自动加 1"发生在**取指令之后、译码之前**
- **易错**：不是执行完才 +1；译码器分析的是**操作码**

---

## Day 2 ｜ 计算机组成（二）：进制与码制

### 进制转换
- **要点**：十→二：整数除 2 取余**倒着读**，小数乘 2 取整**正着读**；二→八每 **3** 位一组、二→十六每 **4** 位一组（从小数点向两边分）
- **算例**：45 = 32+8+4+1 → **101101**；1011011₂ → 0101 1011 → **5B**；0.25 → **0.01₂**
- **易错**：余数忘倒序（错题实录：45 曾写成 110101=53）；自检：权重加和验算
- **口诀**：除二取余倒着读，八三对来十六四
- **2 的幂速查**：1, 2, 4, 8, 16, 32, 64, 128, 256, 512, 1024

### 原码/反码/补码/移码
- **要点**：正数三码合一；负数：原码数值位取反=反码，反码+1=补码；移码=补码符号位取反（用于阶码）
- **算例**：-46（8位）：原 10101110 → 反 11010001 → 补 **11010010**；-1 的补码 = **11111111**
- **易错**：反码忘了 +1；移码不是"取反数值位"
- **口诀**：正数三码一，负数反加一，移码翻个头（翻符号位）

### 定点整数表示范围
- **要点**：原码 -(2ⁿ-1)~+(2ⁿ-1)（0 占两坑）；补码 -2ⁿ~+(2ⁿ-1)（0 唯一，左端多一格）
- **算例**：8 位原码 **-127~+127**；8 位补码 **-128~+127**；16 位补码 **-32768~+32767**
- **口诀**：原码对称缺一格，补码左边多一格

---

## Day 3 ｜ 计算机组成（三）：浮点数、寻址、RISC、流水线

### 浮点数表示 + IEEE 754 单精度
- **要点**：N = 尾数 × 2^阶码；**阶码位数管范围（远近），尾数位数管精度**；IEEE 754 单精度 = 1 符号 + 8 阶码（**真实指数+127**）+ 23 尾数（**打头的 1 隐含不存**）
- **算例**：10.25 → 1010.01₂ → 1.01001×2³ → `0｜10000010｜01001000…0` = **0x41240000**；-6.5 → `1｜10000001｜10100…0` = **0xC0D00000**
- **易错**：阶码忘了 +127；尾数把打头的 1 也存进去
- **口诀**：符号一，阶码八来加 127，尾数二十三，打头 1 不存

### 寻址方式
- **要点**：立即=指令自带数（0 次访存最快）；直接=给地址（1 次）；间接=地址的地址（≥2 次最慢、范围大）；寄存器=寄存器里是数（0 次）；寄存器间接=寄存器里是地址（1 次）；变址=变址寄存器+偏移（数组/循环）
- **算例（固定题面）**：内存[100]=200、内存[200]=500、R1=100、指令地址码 A=100 → 直接=**200**；寄存器间接=**200**；间接=**500**；立即=**100**
- **易错**：直接 vs 寄存器间接答案可能相同但路径不同；间接要跑两趟
- **口诀**：立即自带数，直接给门牌，间接门牌的门牌；寄存器装数，寄存器间接装门牌

### RISC vs CISC
- **要点**：RISC = 指令少、寻址少、单周期、**定长**、流水线、**组合逻辑**、寄存器多；**微程序控制是 CISC**
- **算例**：RISC 特征找异类题——混进"微程序控制"的选项就是答案
- **口诀**：RISC 三少一定长，流水组合寄存器多；微程序是 CISC 别认错

### 指令流水线（概念）
- **要点**：取指→分析→执行三段并行；**单条指令时间不变**，提高整体吞吐；节奏受**最慢阶段**限制，各段等长效率最高
- **算例**：3 段各 1 单位、3 条指令 → 总时间 5 单位（段数+指令数-1）
- **易错**：流水线不加速单条指令；不是串行
- **口诀**：取分执三段跑，单条不变整体快，木桶看最慢一段

---

## Day 4 ｜ 计算机组成（四）：校验码与可靠性

### 奇偶校验
- **要点**：加 1 位使 1 的个数为奇（奇校验）/偶（偶校验）；只能检错、**检不出偶数个位同时错**、不能纠错
- **算例**：1011010（4 个 1）偶校验 → 补 0 放最高位 → **01011010**；收到 00110100（3 个 1）→ 违约 → 能检出
- **口诀**：奇凑奇、偶凑偶，一位能查两位漏，纠错另请海明侯

### 海明码
- **要点**：校验位放第 **1,2,4,8…（2ⁿ）** 位；位数公式 **2ᵏ ≥ n+k+1**；每个校验位管"位置号二进制里含自己权值"的位置，组内偶校验；接收方报警组合拼二进制 = **出错位置号**，取反该位即纠错
- **算例**：数据 1011 → k=3 → 码字 **0110011**；数据 1100 → **0111100**；数据 1001 → **0011001**；演示：0110011 第 5 位错 → P1、P4 报警 → 101=5
- **易错**（错题实录 id 37）：**位置 3 同属 P1+P2 组、位置 7 同属三组**，清点别漏跨组数据位
- **口诀**：校验位站 2ⁿ 位，公式 2ᵏ≥n+k+1；三在 P1P2，五在 P1P4，六在 P2P4，七在三个组

### CRC 循环冗余校验
- **要点**：生成多项式**最高次幂 r = 校验位位数**；数据末尾补 r 个 0，与多项式二进制做**模 2 除法（异或不借位）**，**余数**替换补的 0 拼在**末尾**；只检错不纠错（可检突发错）
- **算例**：10110、G=x³+1（1001）→ 10110**000** ÷ 1001 → 余 **100** → CRC 码 = **10110100**（自检整除）
- **易错**：校验码由**余数**定不是商；全拼末尾不是穿插（穿插是海明码）
- **口诀**：多项式几次幂，校验位就几位；末尾补零模二除，余数替换 CRC

### 可靠性计算
- **要点**：串联 R = R₁×R₂×…（变小）；并联 R = 1−(1−R₁)(1−R₂)…（变大）；混合从里往外（先并联小块再串联）；MTBF = 1/λ
- **算例**：0.9×0.9 串联 = **0.81**；两个 0.9 并联 = **0.99**；两并联组串联 = 0.99×0.99 = **0.9801**；三个 0.9 并联 = 1−0.1³ = **0.999**
- **口诀**：串联连乘全得活，并联一减连乘坏；混合先小块再大块

---

## Day 5 ｜ 计算机组成（五）：流水线计算与 Cache（块3 存储层次/磁盘断点未完）

### 流水线计算
- **要点**：周期 Δt = **max(各段时间)**；总时间 T = **(段数k+条数n−1)×Δt**；加速比 S = 串行时间/T（串行 = n×各段之和）；吞吐率 TP = n/T（最大 1/Δt）
- **算例**：5 段各 10ns、10 条 → Δt=10、T=(5+10−1)×10=**140ns**、串行=500ns、S≈**3.57**；变式：4 段各 20ns、20 条 → T=**460ns**、S=1600/460≈**3.48**
- **易错**：各段**不等长**时周期取最长段，但串行分子用**各段之和**
- **口诀**：周期取最长，总时段数加条数减一；加速比串除流，吞吐条数除总时

### Cache 命中率与映像
- **要点**：平均访存时间 = **h×t缓存 + (1−h)×t主存**；映像三方式——直接=固定位置（硬件简单冲突高）、全相联=任意位置（冲突低硬件贵）、**组相联=折中**（组间直接+组内全相联）
- **算例**：h=90%、tc=10ns、tm=100ns → 0.9×10+0.1×100=**19ns**；h=95%、tc=5、tm=100 → **9.75ns**
- **易错**："任意"=全相联，"组内任意"=组相联，"折中"=组相联
- **口诀**：命中乘快加未中乘慢；直接死板、全相联自由、组相联折中

---

## Day 6 ｜ 操作系统（一）：进程管理（块4 PV操作断点未完）

### 进程三态/五态
- **要点**：三态 = 就绪/运行/阻塞；就绪→运行（调度选中）；运行→就绪（**时间片用完**，被动）；运行→阻塞（**等 I/O，主动**）；阻塞→就绪（I/O 完成，被动）；**阻塞→运行不存在**（必须先回就绪）；五态 = 三态 + 新建 + 终止
- **算例**：进程发起磁盘读并等待 → **运行→阻塞**；时间片用完 → **运行→就绪**
- **口诀**：调度就绪跑，片完回队等；等 I/O 进阻塞，醒来先排队，不能插队跑

### 进程 vs 线程
- **要点**：**进程=资源分配的基本单位**，**线程=调度执行的基本单位**；同进程线程**共享**地址空间/全局变量/打开文件，**独有**栈、寄存器、PC；线程创建切换开销小、独立性弱（崩溃连坐）
- **易错**："线程拥有独立地址空间"✗（地址空间归进程）；"线程切换开销大"✗（更小）
- **口诀**：进程管资源，线程管执行；共享一个大厂房，工具箱各拿各

### 同步与互斥
- **要点**：**同步=协作**（直接制约，等上道工序完成才能开始，接力赛）；**互斥=竞争**（间接制约，抢一次一人用的资源，抢坑位）；信号量 S>0=可用资源数，**S<0 绝对值=排队进程数**；P(S)=申请减1（<0 阻塞）、V(S)=释放加1（≤0 唤醒）
- **算例**：S 初值 2，6 次 P、3 次 V → S=−1 → **1 个进程等待**；S 初值 3，5P+2V → S=0、0 个等待
- **易错**（错题实录 id 40）：把"等上道工序完成"（同步）错判为"竞争"（互斥）
- **口诀**：P 申请减一，减完为负去排队；V 释放加一，加完非正就喊人；负值不是欠资源，绝对值是排队人

### PV 操作·生产者消费者
- **要点**：三信号量 **mutex=1（互斥锁）、empty=n（空位数）、full=0（产品数）**；生产者 P(empty)→P(mutex)→放→V(mutex)→V(full)；消费者 P(full)→P(mutex)→取→V(mutex)→V(empty)；**铁律：先资源后互斥**，反了死锁；V 的顺序功能上随意，但**考试按标准模板选**（先解锁后报告）
- **应试铁律**：PV 序列题永远选与标准模板完全一致的选项，哪怕另一个选项功能上也可行
- **口诀**：生产先看空，消费先看满，动窗口先上锁；先资源后互斥，顺序反了死锁见

---

## Day 7 ｜ 操作系统（二）：死锁、银行家（块3 分页分段未学会，断点）

### 死锁四条件与处理策略
- **要点**：四个**必要**条件 = **互斥、请求保持、不可剥夺、循环等待**（缺一不可，但全占不一定死锁——必要非充分）；三种策略——**预防**（破坏四条件之一，事前）、**避免**（银行家算法，事中判断安全状态）、**检测与解除**（事后剥夺/撤销）
- **算例**："一次性申请全部资源"破"请求保持"= 预防；银行家算法 = 避免
- **易错**："破坏条件属于避免"✗（属于预防）
- **口诀**：互斥请求不剥夺，循环等待死锁锁；一次性申请破保持，银行家边走边躲

### 银行家算法
- **要点**：剩余资源 = 总量−已分配之和；反复找"最多还需要 ≤ 剩余"的进程 → 假装完成并**回收它占有的全部** → 全部走完 = **安全**（顺序即安全序列）；卡住 = 不安全 → 拒绝申请；**不安全 ≠ 必死锁**
- **算例**：总10，P1(占3需5)、P2(占2需3)、P3(占2需4) → 剩3 → P2✓回收→5 → P1✓回收→8 → P3✓ → 安全序列 **P2→P1→P3**；变式：总8 → 安全序列 **P3→P2→P1**（P3→P1→P2 也安全，序列不唯一——学生自主发现 ✓）
- **口诀**：剩余找最饿，回收变富豪，全部能走完，安全序列到

### 分页与分段（重讲后结课 ✓）
- **要点**：分页=固定大小、**一维**地址、物理切、**内部碎片**；分段=不定长、**二维**地址（段号+偏移）、逻辑切、**外部碎片**；段页式=先分段再分页
- **地址变换三步（快解）**：**①拆**：页号=地址÷页大小（取商），偏移=余数（"装盒子"：能装满几盒=页号，剩几颗=偏移）；**②换**：查页表（题目给的，页→块映射是 OS 分配的，不固定）；**③拼**：物理地址=块号×页大小+偏移（偏移原样照抄）
- **算例**：1KB、2053、页2→块5 → 商2余5 → 5×1024+5=**5125**（学生重讲后独立拆换拼 ✓）；快解：4KB=2¹² 时低 12 位是偏移
- **口诀**：一拆二换三拼回；酒店标间一维切，民宿套房二维分；页内碎片段外空

### 页面置换算法
- **要点**：缺页=访问的页不在内存；FIFO=**先进先出**（只认进门顺序）；LRU=**最久未使用**（看过去）；OPT=**将来最久不用**（看未来，理论最优，缺页最少）；一般 OPT ≤ LRU ≤ FIFO
- **算例**：2块、序列 2,3,1,2 → FIFO 4 次、LRU 4 次（学生两问独立答对 ✓）；教学例：2块、1,2,1,3,1 → FIFO 4、LRU 3（差别在第 4 步：LRU 保住刚用过的 1）；经典 Belady 序列 3块/1,2,3,4,1,2,5,1,2,3,4,5 → **FIFO 9 次**（学生小测遗忘后看表学会 ✓）
- **易错**：重新调入的页在 FIFO 里变成"最新"；LRU 每次**访问**都刷新"最近使用"时间
- **口诀**：先进先出排队走，最久不用 LRU 手，OPT 看未来，缺页次数它最少

### 文件系统：索引结点（Day8）
- **要点**：FCB=文件身份证（含文件名）；UNIX 把除文件名外的部分抽出=**索引结点**；目录项只存**文件名+i结点号**（瘦目录）
- **算例**：块 1KB、地址项 4B → 每块 256 个地址；直接 10 + 一级 256 + 二级 256² → 只用直接+一级最大 **266KB**
- **口诀**：目录只存名和号，详细档案结点找；直接十块一级二五六，二级平方往上套

### 位示图（Day8）
- **要点**：1 位=1 磁盘块（1占0空）；**字号=块号÷字长（取整）、位号=mod、反推块号=字号×字长+位号**；从 1 编号先减 1
- **算例**：字长 32、块号 257 → 8 余 1 → 字号 8 位号 1（学生独立答对 ✓）；块号 500 → 15 余 20（小测独立答对 ✓）
- **口诀**：字号整除位取余，反推块号乘加回；从 1 编号先减一

### 磁盘调度（Day8）
- **要点**：SSTF=每次最近的（可饥饿）；SCAN=电梯一个方向到底再回头；移动道数逐段累加
- **算例**：磁头 40，SCAN 向大方向，队列 10/30/55/70 → 40→55→70→30→10 = **90**（学生独立答对 ✓）
- **口诀**：SSTF 拣最近，小心有人饿半死；SCAN 电梯走到底，回头再把剩下理

### 遍历与还原树（Day15 ✓）
- **要点**：前序=根左右、中序=左根右、后序=左右根（名字里"前中后"指根的位置）；层序逐层；**前序第一=根、后序最后=根**；**前+中（或后+中）可还原树，前+后不行**
- **还原三步**：前序段首=根 → 中序里根左边=左子树、右边=右子树 → 递归
- **算例**：前序 ABDECF、中序 DBEACF → 后序 **DEBFCA**（学生独立答对 ✓）
- **口诀**：前中后，念的是根的位置；前中还原递归切

### 哈夫曼树与编码（Day15 ✓）
- **要点**：**每步挑权值最小的两个合并**，新结点权=两家之和；**WPL=Σ(叶子权×深度)** 最小；**无度 1 结点** → n 叶总结点 **2n−1**；编码左 0 右 1 = 前缀码
- **算例**：{1,2,3,4} → 合并 3、6、10 → WPL=**19**（老师带练）；变式 {2,4,5,6} → 合并 6、11、17 → WPL=**34**（学生独立答对 ✓）
- **快捷验算**：WPL = 每次合并产生的新权值之和（免画树）
- **口诀**：俩最小先结婚，孩子权是两家和；WPL 权乘深度，全部加起来最小；哈夫曼没有单孩

### 图与拓扑排序（Day16 ✓）
- **要点**：图=顶点+边；**入度=指向自己的箭头数**；拓扑排序=每次**输出入度 0 的点并删边**（解锁后续）；**有环=无拓扑序列**；序列**不唯一**；DFS 一条路走到黑用**栈**、BFS 圈层扩散用**队列**
- **算例**：A→C、B→C、C→D、A→D → 入度 A=0、B=0、C=2、D=2 → 拓扑 A B C D（合法序列 A B C D 和 B A C D 均可）
- ⚠ 错题实录：数入度**漏数 B**（只答 A）——数入度必须**逐点过一遍**
- **口诀**：入度零就可输出，删边解锁下一个；有环没有拓扑序；DFS 栈走到底，BFS 队列圈圈扩

### 哈希表（Day16，断点：验证题未作答）
- **要点**：哈希函数**除留取余 key%p** 直接定位；**冲突=顺延找空位**（线性探测）；**ASL=各元素比较次数总和÷元素个数**
- **算例**：H=key%7，存 15/22/27 → 15@1（1次）、22 冲突探测到 2（2次）、27@6（1次）→ **ASL=(1+2+1)/3=4/3**
- **口诀**：除留取余定位置，冲突顺延找空位；比较次数逐个数，除以个数得 ASL
- **要点**：**每步挑权值最小的两个合并**，新结点权=两家之和；**WPL=Σ(叶子权×深度)** 最小；**无度 1 结点** → n 叶总结点 **2n−1**；编码左 0 右 1 = 前缀码
- **算例**：{1,2,3,4} → 合并 3、6、10 → WPL=**19**（老师带练）；变式 {2,4,5,6} → 合并 6、11、17 → WPL=**34**（学生独立答对 ✓，权×深度法全对）
- **快捷验算**：WPL = 每次合并产生的新权值之和（免画树）
- **口诀**：俩最小先结婚，孩子权是两家和；WPL 权乘深度，全部加起来最小；哈夫曼没有单孩

### 风险管理与 CMM（Day21 ✓）
- **要点**：**风险暴露 = 概率 × 损失金额**（计算题：25%×80万=20万）；应对四招=规避/转移/减轻/接受；CMM 五级=初始（混乱）→可重复（基本管理）→已定义（标准过程）→已管理（**定量**度量）→优化（**持续**改进）
- **算例**：0.25×80=20 万 → A；"定量管理" → 已管理级 C（学生两问独立答对 ✓）
- **口诀**：初始混乱可重复，定义管理再优化；风险暴露概率乘损失
- **要点**：所有路径逐条求长，**最长 = 关键路径 = 总工期**；关键活动**松弛 0**；非关键活动松弛 = 关键路径长 − 该路径长；**压缩非关键活动无用**
- **算例**：A(3)→B(5)→D(2)=10、A(3)→C(6)→D(2)=11 → 关键路径 **A-C-D**、工期 11、B 松弛 1（学生先答 ABD ✗——"最长"反直觉，引导后改对 ⚠ 9-21 复测）
- **口诀**：最长路径定工期，关键活动零松弛；缩短非关键白费劲，想快先砍最长路

### 函数依赖与候选键（Day11，新增拼图 ✓）
- **要点**：A→B=知道 A 唯一确定 B；候选键=能推出全部属性的**最小**属性集（超键→候选键→主键三兄弟）；求法：**箭头左边起步走**（选从不被推出的属性）→ 逐步推全
- **算例**：R(A,B,C,D) F={A→B,B→C,A→D} → 候选键 **A**（A⁺=全部）；R(A,B,C) F={A→B,B→C} → 候选键 **A**（学生独立答对 ✓）
- **口诀**：箭头左边起步走，推得全部就是键；能推全是超键，最短才算候选

### 范式判定连招（Day11，规则复用口诀本 ✓）
- **连招**：①求候选键 ②判 2NF（组合键查部分依赖，单键天然过）③判 3NF（非主属性传递依赖）④判 BCNF（所有决定方都是候选键）；主属性=在候选键里的
- **算例一**：R(学号,姓名,系,系主任) F={学号→姓名,学号→系,系→系主任} → 候选键学号 → 单键 2NF ✓ → 学号→系→系主任传递 → **最高 2NF**；R(员工号,姓名,部门号,部门名) → 同构 2NF（学生答对+传递链 ✓）
- **算例二**：R(A,B,C) F={AB→C,C→B} → 候选键 {AB,AC} → 全主属性 3NF ✓ → 决定方 C 不是候选键 → **最高 3NF**（BCNF 不满足）
- **算例三（小测）**：R(A,B,C,D) F={A→B,B→C,A→D} → 候选键 A → A→B→C 传递 → **最高 2NF**（学生答对 ✓）
- **口诀**：判定连招一键二部三传递，四查决定方是不是键

### 事务 ACID 与并发控制（Day11 ✓）
- **要点**：**原子性**=全做或全不做；**一致性**=总账不乱；**隔离性**=并发互不干扰（**靠锁**）；**持久性**=提交后断电不丢（靠日志）；并发三问题：丢失更新/读脏数据/不可重复读；**S 锁共享读、X 锁排他写**
- **算例**："全做或全不做"=原子性（学生独立答对 ✓）；"两次读结果不一样"=不可重复读（学生独立答对 ✓）
- **口诀**：原子全或全不，一致总账不乱，隔离互不干扰，持久落袋为安；S 共读 X 独占

### 调度与周转时间（Day8）
- **要点**：**周转=完成−到达**；带权=周转÷运行；响应比=1+等待/服务；SJF 平均周转最短（长作业饥饿）；RR 分时系统
- **算例**：A(3h)、B(1h) 同时 9:00 到 → SJF：B 9-10 周转1、A 10-13 周转4、平均 **2.5h**（学生独立答对 ✓）
- **口诀**：先来先服务最公平，短作业优先最省时；轮转分时片片快，响应比等越久越先

---

## Day 9 ｜ 数据库（一）：三级模式、E-R、关系代数

### 三级模式两级映像
- **要点**：外模式（用户**局部**视图，多个）、概念模式（全局逻辑，1 个）、内模式（物理存储，1 个）；**外↔概映像=逻辑独立性**（表结构变应用不改）；**概↔内映像=物理独立性**（存储变表结构不改）
- **易错实录**：小测"局部数据"错选概念模式（局部=外模式）；"存储结构改变"错选逻辑独立性（概内保物理）→ 9-18 复测
- **口诀**：外菜单内仓库，概念管总谱；外概保逻辑，概内保物理

### E-R 模型
- **要点**：矩形=实体、椭圆=属性、菱形=联系；联系判断"两边都说多个就是 m:n"；转关系模式：1:n 把 1 方主键放 n 方，**m:n 单独建新表**（Day 10 详讲）
- **算例**：医生-病人（两边都多个）→ m:n（学生独立答对 ✓）
- **口诀**：矩实椭属菱联系；一对多多方加钥匙，多对多单独开新表

### 关系代数
- **要点**：**σ 选行（水平）、π 选列（垂直，去重）**；笛卡尔积行数=r×s；自然连接=同名列相等+去重复列
- **算例**：R(3行)×S(4行)=**12 行**（学生独立答对 ✓）；"性别=女的所有行"=选择
- **口诀**：σ 选行 π 选列，笛卡尔积全组合，自然连接同名列

---

## Day 10 ｜ 数据库（二）：SQL 全家桶

### SELECT 基础
- **要点**：SELECT(列，DISTINCT 去重)→FROM(表)→WHERE(挑行)→GROUP BY(分组)→HAVING(挑组)→ORDER BY(排序 DESC 降 ASC 升)；**执行顺序 FROM 最先**，与书写顺序不同
- **算例**：去重=DISTINCT；最先执行=FROM（学生两问独立答对 ✓）
- **口诀**：斯莱克特要菜，弗洛姆找窗口，外尔挑坏行，古鲁拜分盘，海文挑整盘，奥德拜摆盘

### 聚合、分组与子查询
- **要点**：聚合五件套 COUNT/SUM/AVG/MAX/MIN；**WHERE 分组前挑行禁聚合、HAVING 分组后挑组必配聚合**；子查询**先里后外**（配 IN）
- **算例**：人数超 5 的部门 → GROUP BY 部门 HAVING COUNT(\*)>5（学生两问独立答对 ✓）
- **口诀**：WHERE 挑行不带聚合，HAVING 挑组必带聚合；子查询先里后外

### 需求分析与内聚耦合（Day19 ✓）
- **要点**：需求分**功能需求**（做什么）/非功能需求（多快多稳）；结构化分析三件套=DFD+数据字典+E-R；**内聚从低到高：偶逻时过通顺功**；**耦合从低到高：数标控外公内容**；目标=高内聚低耦合
- **算例**："完成一个独立功能"=功能内聚 → D；"传简单数据"=数据耦合 → A（学生两问独立答对 ✓）
- **口诀**：偶逻时过通顺功；数标控外公内容；高内聚低耦合

### 测试覆盖与阶段（Day19 ✓）
- **要点**：**白盒看代码、黑盒看输入输出**；白盒覆盖从弱到强=**语句→判定→条件→判定/条件→条件组合→路径**（路径最强）；黑盒三方法=等价类/边界值/因果图；测试阶段=**单元→集成→确认（α/β）→系统**
- **算例**："不了解内部结构"=黑盒 → B；"覆盖能力最强"=条件组合覆盖 → C（学生两问独立答对 ✓）
- **口诀**：语句判定条件强，组合路径最疯狂；白盒看码黑盒看脸，单元集成确认系统

### 编译过程与解释（Day17 ✓）
- **要点**：六阶段=**词法→语法→语义→中间代码→优化→目标代码**；前三个发现源程序错误；**编译生成目标程序执行快；解释不生成目标程序边翻边执行**
- **算例**：词法=识别单词符号（A ✓）；解释=B 不生成目标程序（学生独立答对 ✓）
- **口诀**：认单词、拆句子、懂意思、写草稿、再润色，最后出成品
- ⚠ 低频标注：文法与推导、有限自动机本轮略过，考前串讲补

### 中缀转后缀（Day17 ✓）
- **要点**：**按优先级补全括号 → 运算符挪右括号外 → 去括号**；后缀求值用**栈**
- **算例**：a−b+c → **a b − c +**（学生独立答对 ✓）；a*(b+c)−d → **a b c + * d −**
- **口诀**：先加括号按优先，运算符挪括号外，括号全去掉，后缀自然来

### 传参（Day17 ✓）
- **要点**：**值传递=副本**（实参不变）；**引用/地址传递=本尊**；C 语言只有值传递
- **算例**：f(int x){x=100} → 输出仍 1（学生独立判断 ✓）；传 &a 才变 100
- **口诀**：值传复印件，随便改不疼；想改真数据，地址递过去
- **要点**：连接=按同名列横向拼（WHERE A.学号=B.学号 或 JOIN ON）；**视图=虚表不存数据**，作用=安全性+简化查询；**GRANT 发权、REVOKE 收权、WITH GRANT OPTION=可转授**
- **算例**：视图选择题 B ✓、转授题 B ✓（学生独立答对 ✓）
- **口诀**：视图虚表不存数，安全简化两用途；GRANT 给权 REVOKE 收，WITH OPTION 能转授

### 模块小测错题实录（Day8，错题本 id41/42）
- **A/D 镜像陷阱**：进程=资源分配单位 vs 线程=调度执行单位——考试用这对镜像互当干扰项，见字先辨"帽子"
- **不安全 ≠ 死锁**：不安全=无保证（可能死锁）；死锁=已发生。银行家承诺"不进不安全态"，"不安全"本身不是判定结果

---

## Day 12 ｜ 巩固日 + 网络（一）

### 独立性配对（顽固点销账中 ✓）
- **终极锚点**：**内模式 = Internal = 物理存储本身** → 概↔内=物理独立性、外↔概=逻辑独立性
- 巩固日三问全对 ✓（9-18 复测确认后彻底销账）

### m:n 独立表（顽固点销账中 ✓）
- 巩固两问全对 ✓：m:n → 关系模式 **3 个**；联系自带属性（库存量）放**联系自己的新表**
- 扎心类比：学生选课的成绩放学生表/课程表都塞不下 → 只能单独建选课表

### OSI 七层与设备
- **要点**：七层=应用/表示/会话/传输/网络/数据链路/物理；TCP/IP 四层=应用+传输+网际+网络接口；**路由器=网络层认 IP、交换机=数据链路层认 MAC、集线器=物理层广播**
- **算例**：路由器→网络层、数据链路层设备→交换机（学生两问独立答对 ✓）
- **口诀**：应表会传网数物；路由选 IP，交换认 MAC，集线器广播

### m:n 属性归属（顽固点，9-18 动手任务后通过）
- **判断标准**：属性描述"谁"就放谁的表——姓名→学生表、课名→课程表；**成绩描述"某学生选某门课"这件事 → 选课表（学号，课程号，成绩）**
- **错例时间线**：连错 4 次（小测/补练/复测/动手任务）后，"眼见为实"（成绩塞学生表导致无法查"数学最高分"）+ 自己复述核心句后才通过 → 9-19 复测确认
- **选课表结构**：`选课表：(学号，课程号，成绩)`，每行描述"一次选课"

---

## Day 14 ｜ 数据结构（一）：链表、栈、队列（压缩版）

### 顺序表 vs 链表
- **要点**：顺序表（数组）**随机存取 O(1)**、插删 **O(n) 挪动**；链表插删 **O(1) 改指针**（定位后）、查找 **O(n) 从头走**；链表有指针域额外开销；选型：多查少改用顺序，多改少查用链表
- **算例**：频繁随机访问+少插删 → 顺序表（学生独立答对 ✓）；链表 p 后插 s → **先接后断**：`s→next=p→next; p→next=s`（学生首答 B 反了——先 p→next=s 会丢后继，引导后理解 ✗ 错题本）
- **口诀**：顺序随机快，插删挪成灾；链表插删快，查找排队来

### 栈与队列
- **要点**：栈=LIFO 摞盘（进出都在栈顶），应用：**递归调用、表达式求值、括号匹配**；队列=FIFO 排队（尾进头出）
- **算例**：递归调用用栈 → B（学生独立答对 ✓）
- **口诀**：栈摞盘队排队

### 循环队列
- **要点**：防假溢出，数组首尾相接，**指针走一步取模 %M**；**元素个数 =（rear−front+M）%M**；判满判空注意 front/rear 相同时的歧义（少存一个元素或设标志位）
- **算例**：M=6、front=1、rear=4 → (4−1+6)%6=**3 个**（学生独立答对 ✓）
- **口诀**：循环取模走，个数后减前加模容量
- **要点**：按**第一段**判类：A=1~126、B=128~191、C=192~223、D=224~239 组播、E=240~255；私有=**10.x、172.16~31.x、192.168.x**；**127.x.x.x 回环**（占位所以 A 类最大 126）
- **算例**：130.24.35.6=B 类（✓）；192.168.10.1 私有不能上公网（✓）
- **答疑实录**：B 类判据=第一段 128~191（不是"172 开头"），172.16~31 只是 B 类私有特区
- **口诀**：一到一二六是 A，一二八到一九一是 B，一九二到二二三 C；私有 10/172.16/192.168；一二七回环自己测
- **A/D 镜像陷阱**：进程=资源分配单位 vs 线程=调度执行单位——考试用这对镜像互当干扰项，见字先辨"帽子"
- **不安全 ≠ 死锁**：不安全=无保证（可能死锁）；死锁=已发生。银行家承诺"不进不安全态"，"不安全"本身不是判定结果

### 磁盘存取时间
- **要点**：存取时间 = **寻道时间 + 旋转延迟 + 传输时间**；转一圈(ms) = **60000 ÷ 转速rpm**；平均旋转延迟 = **半圈时间**；寻道=磁头横着挪到目标磁道，旋转=盘片转着等扇区到磁头下方
- **算例**：7200rpm → 转一圈 60000÷7200≈8.33ms → 平均旋转延迟 **4.17ms**；6000rpm → 一圈 10ms → 平均 **5ms**
- **易错**：平均旋转延迟忘了**除以 2**（是半圈不是整圈）；寻道与旋转混淆

### 📐 公式符号速查（本模块，忘了先翻这里）
| 符号 | 含义 | 记忆钩子 |
|---|---|---|
| Δt | 流水线周期 = **max(各段时间)** | 木桶短板取最长 |
| k / n | 流水线段数 / 指令条数 | k 段 n 条 |
| T | 总时间；等长 T=(k+n−1)×Δt；**不等长 T=各段之和(首条) + (n−1)×Δt** | 首条流完，再等 n−1 个周期 |
| S | 加速比 = 串行时间 ÷ 流水时间 | 串除流 |
| TP | 吞吐率 = n ÷ T（最大 1/Δt） | 条数除总时 |
| h | Cache 命中率（0~1） | h=hit |
| tc / tm | Cache 存取时间 / 主存存取时间 | c=cache 快，m=memory 慢 |
| 平均访存 | **h×tc + (1−h)×tm**（未中只算主存，不再加 tc） | 命中乘快加未中乘慢 |
| rpm | 磁盘每分钟转数 | 转一圈=60000÷rpm |
| R₁,R₂… | 各部件可靠度（0~1） | 串联连乘；并联 1−∏(1−Rᵢ) |
| λ / MTBF | 失效率 / 平均无故障时间 = 1/λ | 互为倒数 |

---

## Day 16（2026-09-18）· 数据结构（三）：哈希表 + 八大排序

### 哈希表（散列）
- **要点**：哈希函数定位置（除留取余 H(key)=key%p，p 常取≤表长的最大质数）；冲突=两数算出同一位置；**线性探测**=冲突顺延下一格找空位（开放定址法）；另一路=**链地址法**（冲突的挂链表）；查找一步到位，平均 O(1)
- **算例（固定）**：H=key%7 依次存 15/22/27 → 15@1（1次）、22 冲突→2（2次）、27@6（1次）→ ASL=(1+2+1)/3=**4/3**；当天验证：存 10/1/8 → 10@3、1@1、8 冲突→2，ASL=4/3（✓ 独立答对）
- **易错**：比较次数=**探测了几次位置**（≠位置号！22 在 2 号位但比较了 2 次）；分母=**元素个数**（≠表长）；必须按题给顺序依次存，先存者占坑挤后存者
- **口诀**：除留取余定位置，冲突顺延找空位；比较次数逐个数，除以个数得 ASL

### 八大排序（核心表 = 考分本体）
| 算法 | 平均 | 最坏 | 空间 | 稳定 |
|---|---|---|---|---|
| 冒泡 / 直接插入 | O(n²) | O(n²) | O(1) | ✅ |
| 简单选择 | O(n²) | O(n²) | O(1) | ❌ |
| 希尔 | ~O(n^1.3) | O(n²) | O(1) | ❌ |
| 快速 | O(nlogn) | ⚠ O(n²) | O(logn) | ❌ |
| 归并 | O(nlogn) | O(nlogn) | ⚠ O(n) | ✅ |
| 堆 | O(nlogn) | O(nlogn) | O(1) | ❌ |
| 基数 | O(d(n+r)) | 同左 | O(r) | ✅ |

- **易错三条**：① 快排最坏退化 O(n²)（序列本来有序时）；最坏仍 O(nlogn) 的只有**归并和堆** ② 唯一要 O(n) 额外空间的是**归并** ③ 序列**基本有序**时冒泡/直接插入反而最快（~O(n)）——考过原题
- **口诀**：快些选堆不稳定（快=快速、些=希尔、选=简单选择、堆=堆排序；表里剩下的全稳定）

### 📐 公式符号速查（本模块）
| 符号 | 含义 | 记忆钩子 |
|---|---|---|
| H(key)=key%p | 哈希函数，余数=位置 | 除留取余 |
| ASL(成功) | 比较次数总和 ÷ 元素个数 | 逐个数再除 |
| WPL | Σ(叶子权值×到根边数)=每次合并新权值之和 | 权乘深度加一遍 |
| n₀ / n₂ | 叶子数 = 度2结点数 + 1 | 差一补一 |

---

# 📐 题眼→公式触发表（看到什么，写什么；持续追加）

> 用法：遮住右两列，只看"题眼"列，能不能 20 秒内默写出公式并解释符号？能→过；不能→翻开对应 Day 重推一遍。
> 推导型公式不背，忘就现场推（每条附 30 秒推导路径）。

| 题眼（题干出现…） | 立刻写 | 推导路径（忘时用） |
|---|---|---|
| 流水线 + n 条指令 + 各段时间 | Δt=max(各段)；T=首条之和+(n−1)×Δt | 第1条流完所有段=相加；之后每 Δt 吐 1 条 → (n−1) 不是 n |
| 命中率 h / Cache / 平均访存 | h×tc+(1−h)×tm | 加权平均：h 走快路，1−h 走慢路；未中不加 tc |
| 可靠度 / 串联 / 并联 | 串联连乘；并联 1−∏(1−R) | 全活才活→乘；全坏才坏→1−乘 |
| 转速 rpm | 一圈=60000÷rpm(ms)；平均旋转延迟=÷2 | 每分钟 60000ms，除 rpm=一圈；平均=半圈 |
| 海明码 / 校验位几位 | 2ᵏ≥n+k+1 取最小 k；校验位在 2ⁿ 位 | 纯记忆，靠口诀"校验位站 2ⁿ 位" |
| 生成多项式 x³+… | 最高次幂=r=校验位数；末尾补 r 个 0 模 2 除，余数拼末尾 | 纯记忆，靠口诀"多项式几次幂…" |
| IEEE754 单精度 | 1 符号+8 阶码(真实指数+127)+23 尾数 | 纯记忆，靠口诀"1 符 8 阶 23 尾" |
| n₀/n₂ 叶子数 | n₀=n₂+1 | N=Σnᵢ 且 N−1=边数=Σ度数 → 移项即得 |
| 完全二叉树 N 求叶子 | n₀=⌈N/2⌉ | N=2n₀−1+n₁，n₁∈{0,1} → N 奶奶奇偶各推一次 |
| 哈希 + ASL / 线性探测 | 逐个算 H=key%p、冲突顺延；ASL=比较次数和÷元素个数 | 比较次数=探测几次（≠位置号）；分母是元素个数 |
| 哈夫曼 / WPL | 每步并最小两个、新结点回队列；WPL=Σ(权×深度)=各次合并新权之和 | 合并顺序错=整题错（实录：3和4抢先并） |
| 循环队列 + 元素个数 | (rear−front+M)%M | 后减前，负数加模 |
| 排序稳定性 / 复杂度对比 | "快些选堆"不稳定；O(nlogn) 三兄弟=快归堆 | 快排最坏退化；归并空间 O(n) |

---

## Day 22 ｜ 软件工程（五）：UML 与设计模式

### UML 图分类与类关系（✅ 三问独立全对）
- **要点**：**静态图=类图/对象图/构件图/部署图；动态图=用例图/顺序图/状态图/活动图/协作图**；类关系从弱到强=**依赖→关联→聚合→组合→泛化→实现**；**聚合=空心菱形可分离**（车队-车）、**组合=实心菱形同生死**（人-心脏）
- **算例**：类图=静态（C ✓）；车队-车=聚合、人-心脏=组合 → B
- **口诀**：静类部构署，动用顺状活协；聚合空心可分离，组合实心同生死

### 设计模式场景速查（✅ 首答 1 错后改对）
- **要点**：三大类——**创建型**（单例/工厂方法/抽象工厂/建造者/原型）管生、**结构型**（适配器/桥接/组合/装饰/外观/享元/代理）管拼、**行为型**（观察者/策略/模板方法/状态/迭代器/命令/责任链/中介者）管跑；考试=**场景关键词对号入座**
- **算例**：全局唯一→单例 ✓；**一对多通知→观察者**（⚠ 首答中介者——观察者=一对多通知、中介者=多对象互相交互找中间人，场景不同）；支付方式可切换→策略 ✓
- **口诀**：创建管生，结构管拼，行为管跑；场景对号就入座

> ⚠ 结构维护备忘（2026-09-20）：本文件标题行曾因插入吞并出现缺失——Day 8、11、13、15、17、21 的 "## Day X" 标题行需在下次维护时恢复（内容大部分在，标题缺失）

## Day 23（2026-09-22）｜ 多媒体与信息安全

### 多媒体容量计算（✅ 位深坑补讲后过，id51）
- **要点**：图像容量=分辨率×位深÷8；音频容量=采样频率×量化位数×声道数×秒数÷8；**颜色数=2^位深**（16色=4位、256色=8位、真彩24位）；256色=每像素1字节可口算；采样定理=采样频率≥2倍最高频；JPEG/MP3/MPEG 有损、PNG/ZIP 无损
- **算例**：800×600、16色 → 240000 字节；10kHz/8位/单声道/10秒 → 100000 字节
- **口诀**：像素乘位深除以八；采样乘量化乘声道乘秒也除八；颜色数是二的位深方
- **易错**："16色"≠16位（是4位）、64色=6位——**先换算位深再代公式**（9-22 两坑实录）

### 信息安全：加密与签名（✅ 加密方向坑补讲后过，id52）
- **要点**：对称（DES/3DES/AES/RC4）同钥匙快；非对称（RSA/ECC/DSA）公私成对；数字信封=对称传数据+非对称传钥匙；摘要（MD5/SHA）不可逆；**保密=收方公钥加密、签名=发方私钥加密摘要（防抵赖非保密）**；防火墙防不了内部攻击与病毒；病毒需宿主/木马开后门/蠕虫无宿主
- **算例**：甲发乙保密文件 → 用**乙的公钥**加密；甲要签名 → 用**甲的私钥**加密摘要
- **口诀**：发私签名，收公加密；公钥锁，私钥开
- **易错**：方向对调（9-22 X1 错、9-22 晚 M3 二错列薄弱，9-25 独立复测）

## Day 24（2026-09-23）｜ 知识产权与标准化

### 知识产权（✅ 全对）
- **要点**：著作权=完成自动产生（登记自愿），作者终生+死后50、法人**首次发表后50**；人身权（署名/修改/完整）**永久**；**职务作品归单位、作者留署名权**；专利=申请审查（**发明20/实用10/外观15**，申请日起算**不可续**）；商标=注册（10年可**无限续展**，期满前12个月+宽展6个月）；商业秘密=秘密性+价值性+保密措施（公开即失）
- **算例**：老张 2020 年去世 → 财产权保护到 **2070 年**；小王职务开发 → 著作权归公司、小王留署名权
- **口诀**：著自动、专商请；终生加五十，二零一零一十五；商标十年可续命

### 标准化（✅ 全对）
- **要点**：GB 强制、GB/T 推荐（T=推）、GJB 军用、Q/ 企业（可严于国标）；**IEEE=802 局域网、IETF=RFC/TCP/IP、W3C=HTML/CSS/XML**、ITU=电信；标准 5 年复审
- **口诀**：GB 强制 T 推荐，Q 是企业 GJB 军；E 太网，F 协议，C 网页
- **易错**：GB/T 不是强制标准；商标可续、专利不可续
