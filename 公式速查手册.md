# 公式速查手册（每日复习专用）

> **用法**：每天开场前扫一遍；公式默写制对答案用本表。每条公式配【直觉】+【迷你算例】+【易错】，易错全部来自真实错题。
> **维护**：每课新公式课后自动追加；学生再错某公式就在【易错】处追加一条实录。

---

## 一、计算机组成

### 1. 图像/音频容量（多媒体）
- 图像：**存储量(B) = 像素总数 × 位深 ÷ 8**
- 音频：**存储量(B) = 采样频率 × 量化位数 × 声道数 × 秒数 ÷ 8**
- 位深锚点：**颜色数 = 2^位深**（16 色=4 位、256 色=8 位、真彩=24 位）；256 色=每像素 1 字节，可口算
- 算例：1280×720×24bit ÷ 8 = 2764800B ≈ **2.6MB**；10kHz、8 位、单声道、10 秒 → 10000×8×1×10÷8 = **100000B**
- ⚠ 易错实录（9-22 两连坑）："800×600、16 色"把位深取成 16 位（应 **4 位**）；"64 色"答 4 位（应 **6 位**）——颜色数与位深两个数字串台，**先换算位深再代公式**

### 2. 折半查找（算法）
- 公式：**最大比较次数 = ⌊log₂n⌋ + 1**
- 算例：n=8 → 3+1 = **4 次**
- 直觉：每次砍半：8→4→2→1

### 3. IEEE 754 单精度浮点
- 公式：**1 符号 + 8 阶码（真实指数+127）+ 23 尾数**，规格化 1.xxxxx×2ⁿ，打头 1 隐含
- 算例：10.25 → `0｜10000010｜01001000…0` = 0x41240000

### 4. 定点整数范围
- 原码：-(2ⁿ-1) ~ +(2ⁿ-1)；补码：**-2ⁿ ~ +(2ⁿ-1)**
- 8 位原码 -127~+127；8 位补码 -128~+127

### 5. 海明码
- 公式：**2ᵏ ≥ n + k + 1**（k=校验位数，取最小）
- 算例：n=8 → k=4（16≥13）
- 易错：数据位站 3~7 号位置，**别漏跨组数据位**（错题实录：位置 3 同属 P1+P2）

### 6. CRC 循环冗余校验
- 步骤：**r=多项式最高次幂 → 数据补 r 个 0 → 模 2 除法（异或）→ 余数替换末尾**
- 算例：10110、G=1001 → 10110000÷1001 余 100 → **10110100**

### 7. 流水线计算 ⚠ 薄弱公式
- **Δt = max(各段时间)** ← "节拍"，由最慢段决定
- **T = 首条各段之和 + (n−1)×Δt**（等长时 = (k+n−1)×Δt）
- 加速比 S = 串行时间 ÷ T（串行 = n×各段之和）；吞吐率 TP = n/T
- 算例：4 段 10/20/15/5ns、16 条 → ①和=50 ②Δt=20 ③T=50+15×20=**350ns**
- ⚠ 易错实录（4 次连环错）：
  - **50 是走全程的，20 是节拍**——①和②是两个不同的数，Δt 必须**现场算 max**，禁止拿①顶替
  - (n−1) = 条数−1，别算成别的

### 8. Cache 平均访存 ⚠ 曾错公式
- 公式：**平均 = h×t_c + (1−h)×t_m**
- 算例：h=95%、tc=10、tm=100 → 0.95×10+0.05×100 = **14.5ns**
- ⚠ 易错实录：
  - 两边都要**乘各自概率**（错过：命中部分漏乘 h 直接写 tc）
  - 未命中代价**只算主存 tm**（错过：多加一遍 tc 得 20ns）

### 9. 磁盘存取时间
- 公式：**存取 = 寻道 + 旋转延迟 + 传输**；**平均旋转延迟 = 60÷(2×转速)**（半圈）
- 算例：6000rpm → 10ms/圈 → 旋转 5ms；寻道 8 + 5 + 传输 1 = **14ms**

### 10. 可靠性
- **串联 R = R₁×R₂×…**（变小）；**并联 R = 1−(1−R₁)(1−R₂)…**（变大）；混合从里往外
- **MTBF = 1/λ**
- 算例：0.9×0.9=0.81；两 0.9 并联=0.99；三 0.9 并联=0.999

---

## 二、操作系统

### 11. PV 信号量
- **P(S)：S−1，S<0 则阻塞**；**V(S)：S+1，S≤0 则唤醒一个**
- **S<0 时 |S| = 排队进程数**（不是欠的资源数）
- 算例：S 初值 2，6P+3V → S=−1 → 1 个等待

### 12. 生产者-消费者（单缓冲）
- 信号量：**mutex=1、empty=n、full=0**
- 生产者：P(empty)→P(mutex)→放→V(mutex)→V(full)；消费者：P(full)→P(mutex)→取→V(mutex)→V(empty)
- ⚠ 铁律：**先资源后互斥**（P 顺序反了死锁）；考试按标准模板选（应试实录：V 顺序功能上随意但选模板）

### 13. 银行家算法
- 步骤：**剩余 = 总量−已分配之和** → 找"还需 ≤ 剩余"的进程 → 假装完成**回收它占有的全部** → 重复
- 安全 = 能全部走完（序列不唯一）；**不安全 ≠ 必死锁**
- 算例：总10，P1(占3需5)P2(占2需3)P3(占2需4) → 剩3 → P2→P1→P3 安全

### 14. 分页地址变换
- **页号 = 逻辑地址 ÷ 页大小（取商）｜偏移 = 取余｜物理 = 块号×页大小 + 偏移**
- 直觉：装盒子——装满几盒=页号，剩几颗=偏移；偏移**原样照抄**
- 算例：1KB、2053、页2→块5 → 商2余5 → 5×1024+5 = **5125**
- 快解：页大小 2¹²（4KB）时，地址二进制低 12 位=偏移

### 15. 位示图
- **字号 = 块号 ÷ 字长（取整）｜位号 = 块号 mod 字长｜反推：块号 = 字号×字长+位号**
- 算例：字长 32、块号 257 → 8 余 1 → 字号 8 位号 1
- ⚠ 从 1 开始编号先减 1

### 16. 磁盘调度移动道数
- 逐段累加相邻两站距离；SSTF=最近的先服务（可饥饿）；SCAN=电梯到底再回头
- 算例：磁头 40，SCAN 先向大方向：40→55→70→30→10 = 15+15+40+20 = **90**

### 17. 调度与周转时间
- **周转时间 = 完成 − 到达**；带权周转 = 周转 ÷ 运行时间；响应比 = 1 + 等待/服务
- SJF 平均周转最短（长作业饥饿）；RR 用于分时系统
- 算例：A(3h)、B(1h) 同时 9:00 到，SJF → B 9-10（周转1）、A 10-13（周转4）、平均 **2.5h**

---

## 三、数据结构

### 18. 循环队列元素个数
- 公式：**(rear − front + M) % M**（M=容量）
- 算例：M=8、front=2、rear=5 → (5−2+8)%8 = **3**
- 易错：rear<front 时直接相减得负，必须**加 M 再取模**

### 19. 二叉树叶子与度 2 结点
- 公式：**n₀ = n₂ + 1**（叶子数 = 度2结点数 + 1）
- 算例：8 个叶子 → n₂ = **7**
- 快推：N=N₀+N₁+N₂ 且 N−1=N₁+2N₂ → 消 N₁ 即得

### 20. 哈夫曼 WPL ⚠ 曾错公式
- 公式：**WPL = Σ（叶子权值 × 到根的边数）= 各次合并产生的新权值之和**
- 步骤：每步合并**当前最小的两个**，**新结点放回队列继续参与比较**
- 算例：{1,2,3,4} → 1+2=3 → 3+3=6 → 4+6=10 → WPL=3+6+10=**19**
- ⚠ 易错实录（9-18 小测答错 23）：并出 3 之后队列是 {3,3,4}，最小两个是 **3 和 3**，不是 3 和 4

### 21. 哈希表 ASL（线性探测）
- 公式：**ASL(成功) = Σ每个元素的比较次数 ÷ 元素个数**
- 存入：H(key)=key%p 定位 → 空则放（1 次）→ 占则顺延下一格（每次挪 1 格算 1 次比较）
- 算例：H=key%7 存 10/1/8 → 10@3(1)、1@1(1)、8 冲突→2(2)，ASL=(1+1+2)/3=**4/3**
- 易错：比较次数=探测位置数（≠位置号）；分母=元素个数（≠表长）；必须按题给顺序存

---

## 待学模块公式（边学边补）
- [ ] 数据结构：完全二叉树 n₀=⌈N/2⌉、图的度（其余已于 9-18 补入第三节）
- [ ] 网络：子网划分、CIDR
- [ ] 数据库：函数依赖、候选键、范式
- [ ] 软件工程：McCabe 环路复杂度 V = E − N + 2、关键路径
