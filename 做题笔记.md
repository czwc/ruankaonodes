# 做题笔记

## 🎵 口诀汇总区（考前最后一眼就看这里）

> 所有专题的速记口诀集中沉淀于此，新笔记中的口诀会同步更新到这里。

### 计算机组成 · 存储器 SRAM/DRAM（2026-08-31）

- **S 快贵不用刷，D 慢便宜要刷新，D 集成更高**
- 用途：SRAM 做 Cache，DRAM 做主存

### 操作系统 · 分页地址变换（2026-08-31）

- **整除得页号，取余留偏移；查表换帧号，拼出新地址**
- 偏移不变，页号换帧号：物理地址 = 帧号 × 页大小 + 偏移

### 数据结构 · 完全二叉树（2026-08-31）

- 叶子定，求总数：**最多乘二，最少乘二减一**
- 总数定，求叶子：**对半向上取整**
- 完全二叉树，单孩结点**要么 0，要么 1**
- 顺序存储：**父 i，左 2i，右 2i+1；空结点也占位，下标不能跳**

### 数据结构 · 树的叶子数通用公式（2026-08-31）

- **所有度数加起来 +1，减去非叶子结点总数 = 叶子数**
- 更快形式：叶子数 = 1 + Σ(度数-1)×该度数结点数（$n_1$ 不出现）
- 二叉树特例即 $n_0 = n_2 + 1$

### 数据结构 · 图（2026-08-31）

- 邻接矩阵**对称 = 无向图**，**不对称 = 有向图**
- 拓扑排序前提：**有向无环图（DAG）**，至少存在一个**入度 = 0** 的点
- **强连通**：任意两点互相可达（u 到 v、v 到 u 都有路）
- 图**有环 = 没有拓扑序列**

### 计算机组成 · IEEE 754 单精度浮点数（2026-09-07）

- **符号一，阶码八来加 127，尾数二十三，打头 1 不存**
  - 📌 逐字解读：**符号**占 **1** 位（0 正 1 负）；**阶码**占 **8** 位且存的是**真实指数 + 127**（偏移量）；**尾数**占 **23** 位（小数点后的部分）；规格化成 1.xxxxx × 2ⁿ 后，小数点前固定的 **1 是隐含位**，不占存储
  - 三步法：十进制→二进制 → 规格化 1.xxxxx×2ⁿ → 拼 S｜E｜M
  - 算例：10.25 = 1010.01₂ = 1.01001×2³ → `0｜10000010｜01001000...0` = 0x41240000
  - -6.5 → `1｜10000001｜10100...0` = 0xC0D00000

### 计算机组成 · 寻址方式（2026-09-07）

- **立即自带数，直接给门牌，间接门牌的门牌；寄存器装数，寄存器间接装门牌**
  - 📌 逐字解读：**立即**=指令里自带操作数（访存 0 次，最快）；**直接**=给出内存地址（1 次）；**间接**=给的是"存放地址的地址"（≥2 次，最慢但范围大）；**寄存器寻址**=寄存器里装的是数（0 次）；**寄存器间接**=寄存器里装的是地址（1 次）；**变址**=变址寄存器+偏移，适合数组/循环
  - 判题套路：给一张内存/寄存器值表，问某寻址方式下操作数是几——先分清"指令给的是数还是门牌"

### 计算机组成 · RISC vs CISC（2026-09-07）

- **RISC 三少一定长：指令少、寻址少、周期少，长度全一样；流水组合寄存器多；微程序是 CISC**
  - 📌 逐字解读：RISC（精简指令集）=指令数量**少**、寻址方式**少**、绝大多数指令**单周期**完成、指令**定长**、用**流水线**+**组合逻辑（硬布线）**控制、**寄存器数量多**；**微程序控制**是 CISC（复杂指令集）的特征——出现在 RISC 选项里就是陷阱
  - 答题套路：RISC 特征题找**异类**（混进来的 CISC 特征就是答案）

### 计算机组成 · 指令流水线（2026-09-07）

- **取分执三段跑，单条不变整体快，木桶看最慢一段**
  - 📌 逐字解读：流水线把指令拆成**取指、分析（译码）、执行**三段并行；**单条指令的执行时间不变**，提高的是多条指令的**整体吞吐率**；节奏受限于**耗时最长的阶段**（瓶颈），各阶段等长时效率最高

### 计算机组成 · 奇偶校验（2026-09-08）

- **奇凑奇、偶凑偶，一位能查两位漏，纠错另请海明侯**
  - 📌 逐字解读：奇校验把 1 的个数**凑成奇数**、偶校验凑**偶数**；**1 位**（奇数个位）出错能查出，**2 位**同时错查不出（奇偶性不变）；**纠错**靠海明码
  - 算例：1011010（4 个 1）偶校验 → 补 0 放最高位 → 01011010

### 计算机组成 · 海明码（2026-09-08）

- **校验位站 2ⁿ 位，公式 2ᵏ≥n+k+1；三在 P1P2，五在 P1P4，六在 P2P4，七在三个组**
  - 📌 逐字解读：校验位放第 **1,2,4,8…** 位；校验位个数 k 由 **2ᵏ ≥ n+k+1** 求最小值；数据位站在位置 3~7，各属多个校验组（位置 3=P1+P2、5=P1+P4、6=P2+P4、7=三组全占），**清点别漏跨组数据位**；接收方报警组合拼二进制 = **出错位置号**
  - 算例：数据 1011 → k=3 → 码字 0110011；数据 1001 → 0011001

### 计算机组成 · CRC 循环冗余校验（2026-09-08）

- **多项式几次幂，校验位就几位；末尾补零模二除，余数替换 CRC**
  - 📌 逐字解读：生成多项式**最高次幂 = 校验位位数 r**；数据末尾**补 r 个 0**，与多项式二进制做**模 2 除法（异或不借位）**，**余数**替换补的 0 拼在**末尾**；只检错不纠错
  - 算例：10110、G=x³+1（1001）→ 补 3 个 0 除之 → 余 100 → CRC = 10110100

### 计算机组成 · 可靠性计算（2026-09-08）

- **串联连乘全得活，并联一减连乘坏；混合先小块再大块**
  - 📌 逐字解读：**串联** R=R₁×R₂×…（全得活，结果变小）；**并联** R=1−(1−R₁)(1−R₂)…（全坏才坏，结果变大）；混合系统从里往外：先算并联小块，再串联大块；MTBF=1/λ
  - 算例：0.9×0.9=0.81（串联）；两个 0.9 并联=0.99；两并联组串联=0.9801；三个 0.9 并联=0.999

### 计算机组成 · 流水线计算与 Cache、磁盘（2026-09-08）

- **周期取最长，总时 k+n−1；不等长首条全加，剩条减一乘周期**
  - 📌 逐字解读：流水线周期 Δt=**各段时间的最大值**（木桶短板）；各段**等长**时总时间 T=(k+n−1)×Δt（k=段数，n=指令条数）；各段**不等长**时 T=**各段之和**（首条流完）+ **(n−1)×Δt**——是 n−1 不是 n！加速比 S=串行时间÷T，吞吐率 TP=n÷T
  - 算例：4 段 10/20/10/20ns、100 条 → 60+99×20=**2040ns**（错写 2060 的教训见错题本）
- **命中乘快加未中乘慢；转一圈六万除转速，平均再砍一半**
  - 📌 逐字解读：Cache 平均访存时间 = **h×tc + (1−h)×tm**（h=命中率，tc=Cache 存取时间，tm=主存存取时间），**未命中只算主存时间，不再加一遍 tc**；磁盘转一圈(ms)=**60000÷转速rpm**，平均旋转延迟=**半圈**（÷2）；寻道=磁头挪到目标磁道，旋转=等扇区转到磁头下
  - 算例：h=90%、tc=10、tm=100 → 9+10=**19ns**；7200rpm → 8.33÷2≈**4.17ms**

### 操作系统 · 死锁四条件与处理策略（2026-09-11）

- **互斥请求不剥夺，循环等待死锁锁；一次性申请破保持，银行家边走边躲**
  - 📌 逐字解读：四条件缩写"互请剥循"= **互斥、请求保持、不可剥夺、循环等待**（缺一不可，但全占不一定死锁——必要非充分）；**预防**=破坏四条件之一（如一次性申请全部资源破"请求保持"，事前）；**避免**=银行家算法边分配边判断安全状态（事中）；死了再剥夺/撤销=检测解除（事后）

### 操作系统 · 银行家算法（2026-09-11）

- **剩余找最饿，回收变富豪，全部能走完，安全序列到**
  - 📌 逐字解读：剩余资源 = 总量−已分配之和；反复找"**最多还需要 ≤ 剩余**"的进程（"最饿"=需求最小的先喂）；它完成后**回收它占有的全部**（剩余变"富豪"）；全部走完=**安全**（顺序即安全序列，不唯一）；中途卡住=不安全 → 拒绝
  - ⚠ 不安全 ≠ 死锁（不安全只是"没保证"；只能选"还需≤剩余"的进程，银行家不做赌注）

### 操作系统 · 分页与分段（2026-09-11）

- **一拆二换三拼回；酒店标间一维切，民宿套房二维分；页内碎片段外空**
  - 📌 逐字解读：地址变换三步——**拆**出页号(商)和偏移(余)、**换**块号（查页表，映射由 OS 分配不固定）、**拼**物理地址=块号×页大小+偏移（**偏移原样照抄**）；分页像**标间**（固定大小、一维地址、页内**内部碎片**）；分段像**套房**（大小不一、二维地址、段间**外部碎片**）
  - 算例：1KB、2053、页2→块5 → 商2余5 → 5×1024+5=5125

### 操作系统 · 页面置换算法（2026-09-11）

- **先进先出排队走，最久不用 LRU 手，OPT 看未来，缺页次数它最少**
  - 📌 逐字解读：**FIFO** 按**进入顺序**淘汰（谁先进谁先走，与是否常用无关）；**LRU** 淘**最久没被使用**的（看过去，每次访问刷新时间）；**OPT** 淘**将来最久不用**的（看未来，理论最优、缺页最少但无法实现）；一般 OPT ≤ LRU ≤ FIFO
  - 经典 Belady 序列（3 块、1,2,3,4,1,2,5,1,2,3,4,5）FIFO 缺页 **9** 次

### 操作系统 · 文件索引结点（2026-09-14）

- **目录只存名和号，详细档案结点找；直接十块一级二五六，二级平方往上套**
  - 📌 逐字解读：目录项只存**文件名 + i 结点号**（瘦目录）；文件详情在**索引结点**（FCB 去名）；寻址嵌套：**10 个直接地址块**、**一级间接**可指 256 块（块大小÷地址大小）、**二级间接** 256×256、三级=立方
  - 算例：块 1KB、地址 4B → 只用直接+一级最大 **266KB**

### 操作系统 · 位示图（2026-09-14）

- **字号整除位取余，反推块号乘加回；从 1 编号先减一**
  - 📌 逐字解读：1 位=1 磁盘块；**字号**=块号**整除**字长、**位号**=块号**取余**；反推 **块号=字号×字长+位号**；题目说"从 1 开始编号"**先减 1** 再套
  - 算例：字长 32、块号 257 → 8 余 1 → 字号 8 位号 1

### 操作系统 · 磁盘调度（2026-09-14）

- **SSTF 拣最近，小心有人饿半死；SCAN 电梯走到底，回头再把剩下理**
  - 📌 逐字解读：**SSTF** 每次**挑寻道距离最短**的请求（远端可能**饥饿**）；**SCAN** 像**电梯**一个方向扫到底再回头（公平）；移动道数=相邻两站距离**逐段累加**
  - 算例：磁头 40，SCAN 向大方向，队列 10/30/55/70 → 40→55→70→30→10 = 90

### 操作系统 · 调度与周转时间（2026-09-14）

- **先来先服务最公平，短作业优先最省时；轮转分时片片快，响应比等越久越先**
  - 📌 逐字解读：**FCFS** 公平但短作业吃亏；**SJF 平均周转时间最短**（长作业可能饥饿）；**RR 时间片轮转**响应快（分时系统标配）；**高响应比**=1+等待/服务，**等待越久优先级越高**（防饥饿）；**周转时间=完成−到达**、带权周转=周转÷运行
  - 算例：A(3h)、B(1h) 同时 9:00 到，SJF → B 9-10（周转1）、A 10-13（周转4）、平均 2.5h

### 数据库 · 三级模式两级映像（2026-09-15）

- **外菜单内仓库，概念管总谱；外概保逻辑，概内保物理**
  - 📌 逐字解读：**外模式**像菜单（用户局部视图，**多个**）；**内模式**像仓库（物理存储，1 个）；**概念模式**像总谱（全局逻辑，1 个）；**外↔概念映像**保**逻辑独立性**（表结构变应用不改）、**概念↔内映像**保**物理独立性**（存储变表结构不改）
  - ⚠ 关键词：**局部**→外模式、全局→概念模式、存储→内模式（错题实录：曾把"局部"选成概念模式）

### 数据库 · E-R 模型（2026-09-15）

- **矩实椭属菱联系；一对一各配对，一对多多方加钥匙，多对多单独开新表**
  - 📌 逐字解读：**矩形**=实体、**椭圆**=属性、**菱形**=联系；联系类型判断"两边都说多个就是 m:n"；转关系模式——1:1 任一方加对方键、1:n 把 1 方主键放进 **n 方**、**m:n 必须单独建新表**
  - 算例：医生-病人两边都"多个" → m:n

### 数据库 · 关系代数（2026-09-15）

- **σ 选行 π 选列，笛卡尔积全组合，自然连接同名列**
  - 📌 逐字解读：**σ**（选择）筛满足条件的**行**（水平）；**π**（投影）挑指定**列**（垂直，自动去重）；**×** 笛卡尔积行数=r×s 全组合；**⋈** 自然连接只拼**同名列相等**的行并**去掉重复列**
  - 算例：笛卡尔积 R(3行)×S(4行)=**12 行**（行数相乘）

### 数据库 · SQL 语句结构（2026-09-15）

- **斯莱克特要菜，弗洛姆找窗口，外尔挑坏行，古鲁拜分盘，海文挑整盘，奥德拜摆盘**
  - 📌 逐字解读：SELECT（要的列，DISTINCT 去重）→ FROM（数据源表）→ WHERE（挑行）→ GROUP BY（分组）→ HAVING（挑组）→ ORDER BY（排序，DESC 降 ASC 升）；**执行顺序**=FROM 先行 → WHERE → GROUP BY → HAVING → SELECT → ORDER BY，**与书写顺序不同**（错不了：FROM 最先）

### 数据库 · 聚合与分组、子查询（2026-09-15）

- **WHERE 挑行不带聚合，HAVING 挑组必带聚合；子查询先里后外**
  - 📌 逐字解读：聚合五件套 COUNT/SUM/AVG/MAX/MIN（一列算一个数）；**WHERE 分组前挑行、禁止写聚合**（那时平均值还没算出来）；**HAVING 分组后挑组、必配聚合**（"每组平均分>80"→GROUP BY 学号 HAVING AVG(分数)>80）；**子查询先执行里层**，结果常配 IN 给外层
  - 算例：人数超 5 的部门 → GROUP BY 部门 **HAVING COUNT(\*)>5**

### 数据库 · 视图与授权（2026-09-15）

- **视图虚表不存数，安全简化两用途；GRANT 给权 REVOKE 收，WITH OPTION 能转授**
  - 📌 逐字解读：**视图是虚表**，本身**不存数据**（查询时实时从基本表取）；两大作用 **安全性（藏列）+ 简化查询**；**GRANT** 发权限、**REVOKE** 收权限；**WITH GRANT OPTION**=被授权者**可以再转授**给别人
  - 语法：CREATE VIEW 视图名 AS SELECT…；GRANT SELECT ON 表 TO 用户 [WITH GRANT OPTION]

### 数据库 · 范式判定（2026-08-31）

- **1NF：单元格不拆分**
- **2NF：字段不能只靠组合主键的一半推出；单主键天然 2NF**
- **3NF：非主属性不能隔层推导（干掉传递依赖）**
- **BCNF：所有决定方都必须是候选键，主属性也不能互相依赖**
- 一句话：1NF 单元格干净丨2NF 没有"沾一半主键"丨3NF 没有二手数据丨BCNF 连主键内部都不互相决定

### 计算机组成 · CPU 组成与寄存器（2026-09-04）

- **算盘指挥**：CPU = 运算器 + 寄存器组 + 控制器（主存**不属于** CPU）
  - 📌 逐字解读：**算**=运算器（负责算），**盘**=寄存器组（放数据），**指挥**=控制器（指挥全局）
- 运算器四件套：**ALU 算，AC 碗（存中间结果），PSW 灯（记进位/溢出状态），通用寄存器组小碗**
- 控制器四件套：**PC 指下条，IR 装现行，译码器翻译，时序器打拍**
  - 📌 逐字解读：PC=程序计数器存**下一条**指令地址；IR=指令寄存器存**当前正在执行**的指令；指令译码器=翻译操作码；时序发生器=发节拍信号
- 取指时序：取完指令 **PC 立即 +1**（不是执行完才加）
- 指令 = **操作码**（干什么）+ 地址码（对谁干）；区分指令类型只看**操作码**

### 计算机组成 · 进制转换（2026-09-04）

- **除二取余倒着读，八三对来十六四**
  - 📌 逐字解读：十进制转二进制 = 不断**除以 2 记下余数**，先算出来的是**低位**，最后要**倒过来**写（错题教训：45 的余数从下往上读才是 101101）；二进制转**八**进制每 **3** 位一组、转**十六**进制每 **4** 位一组，都**从小数点向两边分**，不足补 0
  - 2 的幂速查：1, 2, 4, 8, 16, 32, 64, 128, 256, 512, 1024

### 计算机组成 · 原码/反码/补码/移码（2026-09-04）

- **正数三码一，负数反加一，移码翻个头**
  - 📌 逐字解读：**正数**的原码=反码=补码（三码合一，最省心）；**负数**补码 = 先把原码数值位**逐位取反**得反码，再**加一**；**移码** = 补码的**符号位取反**（"翻个头"就是翻符号位），专用于浮点数**阶码**（因为移码可以直接比大小）
  - 算例（-46，8 位）：46=00101110 → 原码 10101110 → 反码 11010001 → 补码 11010010

### 计算机组成 · 定点整数表示范围（2026-09-04）

- **原码对称缺一格，补码左边多一格**
  - 📌 逐字解读：n+1 位（1 位符号位）下，**原码**正负对称：-(2ⁿ-1) ~ +(2ⁿ-1)，因为 0 占了 +0/-0 两个坑，两端**缺一格**；**补码** 0 只有唯一表示，省下的 1000...0 用来表示最小负数 -2ⁿ，所以**左端多一格**
  - 常考数字：8 位原码 **-127 ~ +127**；8 位补码 **-128 ~ +127**；16 位补码 **-32768 ~ +32767**

### 数据结构 · 栈与队列（2026-09-04 摸底补讲）

- **栈是摞盘（后进先出 LIFO），队是排队（先进先出 FIFO）**

### 数据库 · SQL 三子句顺序（2026-09-04 摸底补讲）

- **WHERE 先挑行，GROUP 再成组，HAVING 挑组**
- WHERE 不能用聚合函数；HAVING 专门筛"分组后"的结果

---

## 2026-08-31｜计算机组成 · SRAM vs DRAM 一句话口诀

### 一句话口诀

> **S 快贵不用刷，D 慢便宜要刷新，D 集成更高**

### 📌 展开对照（口诀背后的考点）

| 对比项 | SRAM（静态存储器） | DRAM（动态存储器） |
| ------ | ------------------ | ------------------ |
| 存储原理 | 触发器 | 电容存电荷 |
| 速度 | 快 | 慢 |
| 价格 | 贵 | 便宜 |
| 是否刷新 | **不需要** | **需要定时刷新**（电容漏电） |
| 集成度 | 低 | **高** |
| 主要用途 | **Cache（高速缓存）** | **主存（内存条）** |

### 🎯 典型真题自测

1. Cache 一般由哪种存储器构成？
   <details>
   <summary>👉 查看答案</summary>

   **SRAM**。快但贵，适合小容量高速场景 -> Cache；主存容量大、要求便宜 -> DRAM。
   </details>

2. DRAM 需要刷新的原因是？
   <details>
   <summary>👉 查看答案</summary>

   DRAM 用**电容**存储电荷，电容会漏电，须**定期刷新**保持数据；SRAM 用触发器，只要不掉电就不丢失，无需刷新。
   </details>

---

## 2026-08-31｜操作系统 · 分页地址变换口诀

### 口诀

> **整除得页号，取余留偏移；查表换帧号，拼出新地址**

### 📌 四步展开（对应口诀）

1. **整除得页号**：页号 = 逻辑地址 ÷ 页大小（取商）
2. **取余留偏移**：页内偏移 = 逻辑地址 mod 页大小（取余）
3. **查表换帧号**：拿页号查页表 -> 得物理帧号（也叫物理块号）
4. **拼出新地址**：物理地址 = 帧号 × 页大小 + 页内偏移

- 关键：**偏移量原样照抄**，变的只是页号 -> 帧号

### ⚡ 快解技巧（页大小是 2 的幂时免除法）

- 页大小 4KB = $2^{12}$ -> 逻辑地址**二进制低 12 位 = 偏移**，其余高位 = 页号
- 页大小 1KB = $2^{10}$ -> 低 10 位 = 偏移
- 页大小 2KB = $2^{11}$ -> 低 11 位 = 偏移

### 🎯 典型真题自测

1. 某分页系统页大小为 4KB，逻辑地址为 8452，页表中页号 2 对应物理块号 8，求物理地址。
   <details>
   <summary>👉 查看答案</summary>

   - 页号 = 8452 ÷ 4096 = **2**（商），偏移 = 8452 - 2×4096 = **260**
   - 物理地址 = 8 × 4096 + 260 = **33028**
   </details>

2. 页大小为 1KB 的分页系统，逻辑地址 2053 的页号和页内偏移分别是？
   <details>
   <summary>👉 查看答案</summary>

   - 2053 ÷ 1024 = 商 **2** 余 **5** -> 页号 2，偏移 5
   - 快解：1KB = $2^{10}$，2053 的二进制低 10 位即偏移
   </details>

---

## 2026-08-31｜数据结构 · 完全二叉树 5 条必考公式（考频 ⭐⭐⭐⭐⭐）

### 基础符号定义

- $n_0$：叶子结点（度 = 0）
- $n_1$：度为 1 的结点（只有 1 个孩子）
- $n_2$：度为 2 的结点（左右孩子都有）
- $N$：整棵树**总结点数**

### 公式 1｜二叉树通用恒等式（所有二叉树都成立）

$$n_0 = n_2 + 1$$

- 推导：$n_2 = n_0 - 1$
- 含义：叶子结点永远比度为 2 的结点**多 1 个**

### 公式 2｜完全二叉树独有约束

$$n_1 = 0 \quad \text{或} \quad n_1 = 1$$

- 重点：完全二叉树**最多只能有 1 个结点只有左孩子**，不可能出现 2 个及以上 $n_1$

### 公式 3｜总结点公式

$$
\begin{aligned}
N &= n_0 + n_1 + n_2 \\
  &= n_0 + n_1 + (n_0 - 1) \\
  &= 2n_0 - 1 + n_1
\end{aligned}
$$

#### 两种极端（选择题高频）

1️⃣ **已知叶子 $n_0$，求最大结点数**（取 $n_1 = 1$）

$$N_{max} = 2n_0$$

例：$n_0 = 124 \Rightarrow N_{max} = 248$

2️⃣ **已知叶子 $n_0$，求最小结点数**（取 $n_1 = 0$）

$$N_{min} = 2n_0 - 1$$

例：$n_0 = 124 \Rightarrow N_{min} = 247$

### 公式 4｜已知总结点 N，反推叶子 $n_0$

$$n_0 = \left\lceil \frac{N}{2} \right\rceil$$

- N 为偶数：$n_0 = N/2$，此时 $n_1 = 1$
- N 为奇数：$n_0 = (N+1)/2$，此时 $n_1 = 0$

### 公式 5｜顺序存储下标关系（从 1 开始编号）

> **父 i，左 2i，右 2i+1；空节点也要占位置，不能跳过下标**

- 父结点下标 = $\lfloor i/2 \rfloor$
- 判断有无孩子：$2i > N$ 时 i 是**叶子**（无左孩子）；$2i+1 > N$ 时**只有左孩子**
- **衍生结论**：N 个结点中，**最后一个非叶子（分支）结点编号 = $\lfloor N/2 \rfloor$**，其后全是叶子
- 变体（从 0 开始编号，如 C 数组）：左 $2i+1$，右 $2i+2$，父 $\lfloor (i-1)/2 \rfloor$

### ✨ 速记口诀

> - 叶子定，求总数：**最多乘二，最少乘二减一**
> - 总数定，求叶子：**对半向上取整**
> - 完全二叉树，单孩结点**要么 0，要么 1**
> - 顺序存储：**父 i 左 2i 右 2i+1，空位不能跳**

### 🎯 典型真题自测

1. 某完全二叉树共有 700 个结点，则叶子结点数为多少？
   <details>
   <summary>👉 查看答案</summary>

   $n_0 = \lceil 700/2 \rceil = 350$（N 为偶数，$n_1 = 1$）
   </details>

2. 一棵完全二叉树上有 701 个结点，则叶子结点数为多少？
   <details>
   <summary>👉 查看答案</summary>

   $n_0 = \lceil 701/2 \rceil = 351$（N 为奇数，$n_1 = 0$）
   </details>

3. 700 个结点的完全二叉树按层序从 1 编号，编号为 100 的结点其左、右孩子编号分别是多少？
   <details>
   <summary>👉 查看答案</summary>

   左孩子 $2 \times 100 = 200$，右孩子 $2 \times 100 + 1 = 201$；因 $201 \le 700$，两个孩子都存在。
   </details>

4. 700 个结点的完全二叉树按层序从 1 编号，最后一个非叶子结点的编号是多少？
   <details>
   <summary>👉 查看答案</summary>

   $\lfloor 700/2 \rfloor = 350$，编号 351~700 全是叶子（共 350 个，与公式 4 呼应）。
   </details>

---

## 2026-08-31｜数据结构 · 树的叶子数通用公式

### 速记公式（直接背）

$$n_0 = 1 + \sum_{i=1}^k i \times n_i - \sum_{i=1}^k n_i$$

- $n_i$：度为 $i$ 的结点个数（$n_1$ = 只有 1 个孩子的结点数，$n_2$ = 有 2 个孩子的……）
- 口诀：**所有度数加起来 +1，减去非叶子结点总数 = 叶子数**

### 📌 更简形式（做题更快）

$$n_0 = 1 + \sum_{i=2}^k (i-1) \times n_i$$

- 即：**叶子数 = 1 + Σ(度数 - 1) × 该度数结点数**，$n_1$ 根本不出现
- 二叉树特例：$n_0 = 1 + n_2$，正是经典恒等式 $n_0 = n_2 + 1$ -- 本公式是它的**推广**

### 🎯 典型真题自测

1. 设树 T 的度为 4，其中度为 1、2、3、4 的结点个数分别为 4、2、1、1，则 T 中的叶子结点数为多少？
   <details>
   <summary>👉 查看答案</summary>

   - 原式：$n_0 = 1 + (1×4 + 2×2 + 3×1 + 4×1) - (4+2+1+1) = 1 + 15 - 8 = 8$
   - 快解：$n_0 = 1 + 1×2 + 2×1 + 3×1 = 8$
   </details>

2. 一棵度为 3 的树中，度为 3 的结点有 2 个，度为 2 的结点有 1 个（度为 1 的结点数未知），则叶子结点数为多少？
   <details>
   <summary>👉 查看答案</summary>

   **6**。$n_1$ 不影响结果：$n_0 = 1 + 1×1 + 2×2 = 6$。
   </details>

---

## 2026-08-31｜数据结构 · 图论基础速记

### 速记口诀

1. 邻接矩阵**对称 = 无向图**；**不对称 = 有向图**
2. 拓扑排序存在的前提：**有向无环图（DAG）**，必须至少存在一个**入度 = 0** 的点
3. **强连通**：任意 u 到 v、v 到 u 都有路
4. 一旦图存在环：**没有拓扑序列**

### 📌 关联结论（做题常用）

- 无向图：顶点 i 的**度 = 邻接矩阵第 i 行（列）元素之和**（矩阵对称，行列相同）
- 有向图：**第 i 行之和 = 出度**，**第 i 列之和 = 入度**
- n 个顶点的**无向完全图**：边数 = $n(n-1)/2$
- n 个顶点的**有向完全图**：弧数 = $n(n-1)$
- 拓扑序列**不唯一**；做法：每步删去入度为 0 的点及其出边

### 🎯 典型真题自测

1. 若有向图的拓扑序列不存在，则该图一定？
   <details>
   <summary>👉 查看答案</summary>

   **存在环**。拓扑排序仅对 DAG 成立；有环则环上各顶点入度永远 > 0，无法删空。
   </details>

2. 37 个结点的有向完全图有多少条弧？
   <details>
   <summary>👉 查看答案</summary>

   $n(n-1) = 37 \times 36 = 1332$ 条。
   </details>

---

## 2026-08-31｜数据库 · 范式判定 1NF / 2NF / 3NF / BCNF（考频 ⭐⭐⭐⭐⭐）

### 基础名词先看懂

- **主键**：唯一标识一行的属性
- **主属性**：出现在候选键里的字段
- **非主属性**：不在候选键里的字段
- **部分依赖**：主键是组合键，**只靠主键一部分**就能推出某个字段
- **传递依赖**：$A \to B,\ B \to C$，且 B 不反过来推 A，则 C 传递依赖 A

### 四条范式判定规则（逐条往上套）

#### ✅ 1NF（第一范式）

**规则：列不可再分，单元格只能存单个值**

- 禁止：一个格子写多个数据（比如地址 `福建,厦门` 放同一格）
- 只要是一张正常设计的二维表，基本都满足 1NF

#### ✅ 2NF（第二范式）

**前提：先满足 1NF ｜ 规则：消除「非主属性对主键的部分函数依赖」**

1. 主键**是单个字段**：天然满足 2NF
2. 主键**是组合键 (A,B)**：若 $(A,B) \to C$ 且 $A \to C$（C 只依赖 A）-> **部分依赖，不满足 2NF**

- 2NF 只禁止【组合主键带来的局部依赖】，**传递依赖依然允许**

#### ✅ 3NF（第三范式）

**前提：先满足 2NF ｜ 规则：消除「非主属性对主键的传递依赖」**

- 不允许出现：主键 $\to X$，$X \to$ 非主属性
- 做题实例：教师号 -> 岗位，岗位 -> 薪资。薪资是非主属性，传递依赖主键 -> **不满足 3NF，只到 2NF**
- 达不到 3NF 的后果：**数据冗余、插入异常、删除异常、修改异常**

#### ✅ BCNF（巴斯-科德范式，增强版 3NF）

**前提：先满足 3NF ｜ 规则：消除「主属性之间的传递 / 部分依赖」**

- 3NF 只管**非主属性**；BCNF 连**主属性之间的依赖**都要清理干净
- 软考小结论：**BCNF 一定是 3NF；3NF 不一定是 BCNF**

### 🧾 极简判断口诀（做题直接背）

1. **1NF：单元格不拆分**
2. **2NF：组合主键，字段不能只靠主键一半推出；单主键天然 2NF**
3. **3NF：非主属性不能隔一层推导（干掉传递依赖）**
4. **BCNF：所有决定方都必须是候选键，主属性也不能互相依赖**

**一句话快速区分**

- 1NF：单元格干净
- 2NF：没有"沾一半主键就能确定的值"
- 3NF：**没有二手数据（传递得来的值）**
- BCNF：连主键内部都不能互相决定

### 🎯 典型真题自测

1. 设关系模式 R(A, B, C)，函数依赖 F = {A->B, B->C}，则 R 最高属于第几范式？
   <details>
   <summary>👉 查看答案</summary>

   **2NF**。单属性主键 A 天然满足 2NF；但 A->B->C，C 传递依赖 A -> 不满足 3NF。
   </details>

2. 设关系模式 R(A, B, C)，函数依赖 F = {AB->C, C->B}，则 R 最高属于？
   <details>
   <summary>👉 查看答案</summary>

   **3NF**。候选键为 AB 和 AC，A、B、C 全是主属性（无非主属性）-> 满足 3NF；但 C->B 的决定方 C 不是候选键 -> 不满足 BCNF。
   </details>

---
