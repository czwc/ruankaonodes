# 标准讲义库（固定算例 + 固定验证题）

> **用途**：保证任何会话/任何设备讲同一知识块时，口径、算例、出题完全一致。
> **格式**：每个块 = 固定算例（讲解用，一步不跳）+ 固定验证题（讲完必出）+ 备选变式题。
> **维护规则**：讲到一个新块，课后必须把当次用的算例和题沉淀进来；学生错题附错因，作为下次讲解的陷阱提示。
> **状态标记**：✅=已实际授课验证；🆕=新沉淀待下次验证。

---

## Day 1 · 计算机组成（一）

### 块1 CPU 组成 ✅
- **验证题**：下列不属于 CPU 组成部分的是：运算器 / 控制器 / **主存储器** / 寄存器组 → C
- **陷阱**：主存在 CPU 外；"Cache 属于 CPU"也是常见干扰项（严格说在 CPU 芯片内但体系结构上独立于 CPU 核心，题目问"CPU 两大部件"时答运算器+控制器）

### 块2 运算器四大件 ✅
- **验证题**：为 ALU 提供并暂存运算中间结果的部件 → **累加器 AC**（干扰项：IR/PC/PSW）
- **陷阱**：PSW 记录状态（进位/溢出/零标志），不存数据

### 块3 控制器四大件 ✅
- **验证题**："PC 自动加 1"发生在 → **取指令之后、译码之前**（B）
- **陷阱**：不是"执行完才加 1"；IR 存当前指令，PC 存下条地址

## Day 2 · 计算机组成（二）

### 块1 进制转换 ✅
- **算例**：45 → 101101：45=32+8+4+1（权重法）或除2取余：45÷2=22余**1**、22÷2=11余**0**、11÷2=5余**1**、5÷2=2余**1**、2÷2=1余**0**、1÷2=0余**1** → 倒着读 **101101**
- **学生错题实录**：曾错选 110101（余数正着读）；错因=忘倒序。自检法：奇数末位必为 1；权重加和验算
- **验证题**：37 → 100101（复习通过）；1011011₂ → 0101 1011 → **5B**（十六进制，4 位分组）
- **变式题**：0.25 → 0.01₂（乘 2 取整正着读）

### 块2 原码/反码/补码/移码 ✅
- **算例**：-46（8 位）：46=00101110 → 原码 10101110 → 反码 11010001 → 补码 **11010010**
- **验证题**：-46 的补码 → B（11010010）；干扰项 A=反码、C=原码
- **变式题**：-1 的补码 = **11111111**（全 1 即 -1）

### 块3 定点数表示范围 ✅
- **算例**：8 位：原码 -127~+127（0 占两坑）；补码 -128~+127（0 唯一，左端多一格）
- **验证题**：8 位原码范围 → **B（-127~+127）**

## Day 3 · 计算机组成（三）

### 块1 浮点数表示 + IEEE 754 ✅
- **概念题**：表示范围由**阶码位数**决定，精度由**尾数位数**决定（学生曾首答晃：选尾数——复习包见 progress 队列）
- **带练算例（固定）**：10.25 → 1010.01₂ → 1.01001×2³ → S=0，E=3+127=130=10000010，M=01001000…0 → `0｜10000010｜01001000000000000000000` = **0x41240000**
- **变式题（固定）**：-6.5 → 110.1₂ → 1.101×2² → `1｜10000001｜10100000000000000000000` = **0xC0D00000**（学生已独立答对 ✅）
- **陷阱**：阶码是"真实指数+127"不是移码；十六进制按 4 位分段拼

### 块2 寻址方式 ✅
- **固定题面（一张表打天下）**：内存[100]=200，内存[200]=500；R1=100；指令地址码 A=100
- **验证题**：直接寻址=**200**；寄存器间接寻址=**200**；间接寻址=**500**（学生全对 ✅）
- **变式题**：立即寻址下操作数=**100 本身**（数在指令里）
- **陷阱**：直接 vs 寄存器间接答案可能相同但路径不同；"间接"要跑两趟

### 块3 RISC vs CISC ✅
- **验证题**：不属于 RISC 的是 → **采用微程序控制**；关于 RISC 正确的是 → **大多数指令单周期完成**（学生两次全对 ✅）
- **答题套路**：找异类——定长/少/单周期/流水线/组合逻辑/寄存器多是 RISC 标签；"微程序"= CISC

### 块4 指令流水线 ✅
- **固定工作图**：3 阶段（取指/分析/执行）各 1 单位，3 条指令共 **5 个单位时间**（阶段数+指令数-1）
- **验证题**：流水线说法正确的是 → **各阶段执行时间相同时效率最高**（学生首答 D 错、引导后改对 ⚠ 复测包见 progress 队列）
- **陷阱**：流水线不缩短单条指令时间；不是串行执行；瓶颈=最慢阶段

## Day 5 · 计算机组成（五）

### 块1 流水线计算 ✅
- **算例（固定）**：5 段各 10ns、10 条 → Δt=10、T=(5+10−1)×10=140ns、串行=500ns、S≈3.57、TP=10/140；变式算例：4 段各 20ns、20 条 → T=460ns、S=1600/460≈3.48（学生三步独立答对 ✅）
- **验证题（固定）**：4 段各 20ns 执行 20 条 → 周期 20ns、总时 460ns、加速比 ≈3.48
- **变式预警**：各段不等长（1/2/3ns）→ 周期=max=3ns；串行分子用各段之和（6n）不是最长×段数

### 块2 Cache 命中率与映像 ✅
- **算例（固定）**：h=90%、tc=10、tm=100 → 19ns；h=95%、tc=5、tm=100 → 9.75ns（学生两问独立答对 ✅）
- **验证题（固定）**：h=95% 计算 9.75ns；"任意位置"=全相联（B）；"组内任意/折中"=组相联
- **口诀**：命中乘快加未中乘慢；直接死板、全相联自由、组相联折中

### 块3 存储层次 + 磁盘 🆕（断点：验证题未作答）
- **算例（固定）**：6000rpm → 一圈 60/6000=10ms → 平均旋转延迟=半圈 5ms → 存取=寻道8+旋转5+传输1=14ms
- **验证题（待学生作答）**：7200rpm → 平均旋转延迟 60/7200/2 ≈ **4.17ms**；磁头移动到目标磁道=**寻道时间**
- **位示图**：Day 8 详讲，本块只预热"1 位表示 1 块占用状态"
- **口诀**：寻道转半圈再传输；转速定半圈，六十除转速再减半

## Day 10 · 数据库（二）：SQL

### 块1 SELECT 基础 ✅
- **验证题（固定）**：去重=DISTINCT → B；最先执行=FROM → C（学生独立答对 ✅）
- **口诀**：SELECT 六子书写顺序 vs 执行顺序（FROM 最先）

### 块2 聚合分组与子查询 ✅
- **验证题（固定）**：每部门人数 → GROUP BY 部门（B）；人数>5 → HAVING COUNT(*)>5（B）（学生两问独立答对 ✅）
- **口诀**：WHERE 挑行不带聚合，HAVING 挑组必带聚合；子查询先里后外

### 块3 连接视图授权 ✅
- **验证题（固定）**：视图正确说法 → B（简化查询+安全）；WITH GRANT OPTION → B（可转授）（学生两问独立答对 ✅）
- **口诀**：视图虚表不存数，安全简化两用途；GRANT 给权 REVOKE 收，WITH OPTION 能转授

## Day 9 · 数据库（一）

### 块1 三级模式两级映像 ✅（验证题引导后答对，考点入 9-18 复测）
- **算例（固定）**："用户看到的局部数据"→**外模式**；"存储结构改变应用不改"→**物理独立性**（概念↔内映像）
- **陷阱**："局部"vs"全局"关键词；逻辑/物理独立性配对（错题实录：双错后引导改对）

### 块2 E-R 模型 ✅
- **验证题（固定）**：菱形=联系 → C；医生-病人两边都"多个" → m:n → C（学生独立答对 ✅）
- **口诀**：矩实椭属菱联系；判断法"两边都说多个就是 m:n"

### 块3 关系代数 ✅
- **验证题（固定）**："性别=女的所有行"=选择 → A；笛卡尔积 3×4=12 行 → B（学生独立答对 ✅）
- **口诀**：σ 选行 π 选列，笛卡尔积全组合，自然连接同名列

## Day 15 · 数据结构（二）

### 块1 二叉树公式全家桶 ✅（2026-09-18 教学法升级）
- **标准讲法（学生反馈后升级，永久生效）**：**n₀/n₁/n₂ 等符号禁止裸用**——必须先给"黑话↔人话"对照表（n₀=没孩子的结点数=叶子；n₁=只有 1 个孩子；n₂=有 2 个孩子），再引入符号；题干也要翻译成人话再解
- **公式全家桶**：①叶子数=双孩结点数+1（n₀=n₂+1）②总数=零孩+单孩+双孩（N=2n₀−1+n₁）③完全二叉树单孩最多 1 个 ④深度 k 最多 2ᵏ−1 结点 ⑤n 结点完全二叉树深度=⌈log₂(n+1)⌉（⚠ 用"2ᵏ−1≥n 装得下"验算最稳）
- **算例（人话版）**：20 个没孩子+30 个单孩 → 双孩=19、总数=69、深度=⌈log₂70⌉=**7 层**（示范中走了弯路：先答 6 后用"2ᵏ−1≥70"验算纠正——验算习惯要传给学生）
- **口诀**：叶比双孩多一个，总数双叶加单孩；深度要看装得下，二幂减一比一比

## Day 14 · 数据结构（一）

### 块1 顺序表 vs 链表 ✅（错题本 id48）
- **验证题（固定）**：频繁随机访问+少插删 → 顺序表（A，学生独立答对 ✓）；单链表 p 后插 s → **A：先 s→next=p→next 接后继，再 p→next=s**（学生首答 B 反了——先改 p→next 丢后继，引导后理解 ✗）
- **铁律**：链表插入**先接后断**
- **口诀**：顺序随机快，插删挪成灾；链表插删快，查找排队来

### 块2+3 栈、队列与循环队列 ✅
- **验证题（固定）**：M=6、front=1、rear=4 → 元素 **3 个**（独立答对 ✓）；递归调用用**栈** → B（独立答对 ✓）
- **口诀**：栈摞盘队排队；循环取模走，个数后减前加模容量
- **应用清单**：栈=递归/表达式求值/括号匹配；队列=FIFO 尾进头出

### 教学法记录：m:n 属性归属（顽固点，Day 11→14 四连错后通过）
- **有效方法（2026-09-18）**：①三问引导"属性描述谁"（姓名→学生表、课名→课程表）②**眼见为实**：成绩塞学生表一行（"90,85,78"）导致无法查询 → ③学生**自己复述**核心句"成绩是'某个学生选某门课'的属性，不是学生一个人的属性"
- **复测计划**：9-19 确认题（换场景：医生-病人 m:n 带"诊断记录"属性放哪）

### 块2 遍历与还原树 ✅
- **验证题（固定）**：前序 ABDECF、中序 DBEACF → 后序 **DEBFCA**（A，学生独立答对 ✅，且能说出"先确定根 A"）
- **还原三步**：前序段首=根 → 中序切左右 → 递归
- **口诀**：前中后，念的是根的位置；前中还原递归切

### 块3 哈夫曼树与编码 ✅
- **验证题（固定）**：{1,2,3,4} 带练 → WPL **19**；变式 {2,4,5,6} → 合并 6/11/17 → WPL **34**（学生独立答对 ✅，权×深度法全对）
- **快捷验算**：WPL = 每次合并产生的新权值之和（免画树）
- **口诀**：俩最小先结婚，孩子权是两家和；WPL 权乘深度，全部加起来最小；哈夫曼没有单孩
- **⚠ 复现实录（2026-09-18 模块小测）**：同款题 {1,2,3,4} 重出，学生答 23（正确 19）——Day15 曾独立答对但已遗忘，错因=并出 3 后拿 3 和 4 合并（新结点忘放回队列）。已入错题本 id49，9-21 独立复测；变式 {2,3,4,5}→28 引导后答对 ✓

## Day 16 · 数据结构（三）：图与哈希与排序 ✅（2026-09-18 全部完成）

### 块1 图与拓扑排序 ✅（重讲零基础版后完成）
- **算例（固定）**：A→C、B→C、C→D、A→D → 入度 A=0/B=0/C=2/D=2 → 拓扑 A B C D（A C B D 亦合法）
- **验证题**：入度 0 的顶点 → A 和 **B**（学生漏数 B ⚠ 记录：数入度必须逐点过）；拓扑序列 → A B C D（A，但 B 选项 B A C D 也合法——出题瑕疵已向学生坦白）
- **口诀**：入度零就可输出，删边解锁下一个；有环没有拓扑序；DFS 栈走到底，BFS 队列圈圈扩

### 块2 哈希表 ✅（2026-09-18 验证题独立答对，销账）
- **算例（固定）**：H=key%7，存 15/22/27 → 15@1（1次）、22 冲突探测到 2（2次）、27@6（1次）→ **ASL=(1+2+1)/3=4/3**
- **验证题 ✅**：H=key%7 存 10/1/8 → 10@3、1@1、8 冲突探测 2 → ASL=(1+1+2)/3=**4/3**（学生独立答对 ✓，"比较次数≠位置号"的坑自己绕开了）
- **复习包变式**：H=key%7 存 5/12/19 → 5@5、12@6、19 连续冲突绕回 0 → ASL=(1+2+3)/3=**2**（考绕回）
- **口诀**：除留取余定位置，冲突顺延找空位；比较次数逐个数，除以个数得 ASL

### 块3 八大排序 ✅（2026-09-18 讲授，验证题独立答对）
- **核心表**：平均/最坏/空间/稳定——冒泡·直接插入 O(n²)/O(1)/稳定；简单选择 O(n²)/O(1)/不稳；希尔 ~O(n^1.3)/O(1)/不稳；快速 O(nlogn)/最坏 O(n²)/O(logn)/不稳；归并 O(nlogn)/O(nlogn)/**O(n)空间**/稳定；堆 O(nlogn)/O(1)/不稳；基数 O(d(n+r))/O(r)/稳定
- **口诀**：快些选堆不稳定（快=快速、些=希尔、选=简单选择、堆=堆排序；其余全稳定）
- **验证题（固定）**：正确说法 → B"归并稳定且最坏 O(nlogn)"（学生独立答对 ✓）；干扰项：A 快排最坏退化 O(n²)、C"选"在口诀里必不稳、D 堆排序空间 O(1)（要 O(n) 的是归并）
- **易错补充**：序列基本有序时冒泡/直接插入最快（~O(n)）——考过原题

## Day 12 · 巩固日 + 网络（一）

### 巩固关卡一：独立性配对 ✅（三问全对，锚点法生效）
- 终极锚点：**内模式 = Internal = 物理存储本身** → 概↔内=物理独立性，外↔概=逻辑独立性
- 三问：内模式管什么→物理存储；存储变→物理独立性；表结构变→逻辑独立性（学生全对 ✓，待 9-18 复测销账）

### 巩固关卡二：m:n 独立表 ✅（两问全对）
- Q1：学生-课程 m:n → 关系模式 3 个 ✓；Q2：仓库-零件 m:n 联系带"库存量" → 放**联系自己的新关系模式**（C）✓
- 待 9-18 复测销账

### 块1 OSI 七层与设备 ✅
- **验证题（固定）**：路由器=网络层 → C；数据链路层设备=交换机 → C（学生两问独立答对 ✅）
- **口诀**：应表会传网数物；路由选 IP，交换认 MAC，集线器广播
- TCP/IP 四层 = 应用(7~5)+传输(4)+网际(3)+网络接口(2+1)

### 块2 IP 地址分类 ✅（含答疑实录）
- **验证题（固定）**：B 类=第一段 128~191 → 130.24.35.6 选 B；"不能分配给公网"=192.168 私有 → C（第二问独立答对 ✅）
- **学生答疑实录（2026-09-17）**：把"B 类"误关联"172 开头"——纠正：**B 类判据=第一段 128~191**，172.16~31 只是 B 类的私有特区，130 也是 B 类。已入总笔记
- **口诀**：一到一二六是 A，一二八到一九一是 B，一九二到二二三 C；私有 10/172.16/192.168；127 回环

## Day 11 · 数据库（三）：范式与事务

### 块1 函数依赖与候选键 ✅
- **算例（固定）**：R(A,B,C,D) F={A→B,B→C,A→D} → 起点属性 A → A⁺=全部 → 候选键 **A**；变式 R(A,B,C) F={A→B,B→C} → 候选键 **A**（学生两问独立答对 ✓）
- **口诀**：箭头左边起步走，推得全部就是键；能推全是超键，最短才算候选

### 块2 范式判定连招 ✅
- **标准讲法（2026-09-16 学生反馈后升级）**：判定必须**从 1NF 逐级演示到 BCNF，每级写出"判什么+结论"**，禁止只报"最高 2NF"跳级
- **逐级骨架**：1NF=格子原子（正常表天然过）｜2NF=非主属性有无**部分依赖**（单键/无组合键天然过）｜3NF=非主属性有无**传递依赖**（键→A→B 链）｜BCNF=**每个决定方都是候选键吗**；第一级失败即停，前一级=最高范式
- **算例一（卡 2NF，逐级演示）**：R(学号,姓名,系,系主任) F={学号→姓名,学号→系,系→系主任}：主属性=学号，非主属性=姓名/系/系主任 → 1NF✓ → 2NF✓（单键无部分依赖）→ 3NF✗（学号→系→系主任传递）→ **最高 2NF**；同构：R(员工号,姓名,部门号,部门名) → 2NF（学生答对+传递链 ✓）
- **算例二（到 BCNF 门口，逐级演示）**：R(A,B,C) F={AB→C,C→B}：候选键{AB,AC}，全主属性 → 1NF✓ 2NF✓ 3NF✓（无非主属性可判）→ BCNF✗（C→B 决定方 C 非候选键）→ **最高 3NF**
- **算例三（小测）**：R(A,B,C,D) F={A→B,B→C,A→D} → 候选键 A → A→B→C 传递 → **最高 2NF**（学生答对 ✓）
- **口诀**：判定连招一键二部三传递，四查决定方是不是键

### 块3 事务 ACID 与并发控制 ✅
- **验证题（固定）**："全做或全不做"=原子性 → C；"两次读结果不一样"=不可重复读 → C（学生两问独立答对 ✅）
- **口诀**：原子全或全不，一致总账不乱，隔离互不干扰，持久落袋为安；S 共读 X 独占

## Day 6 · 操作系统（一）

### 块1 进程三态/五态 ✅
- **验证题（固定）**：发起磁盘读并等待 → **运行→阻塞**（B）；时间片用完 → **运行→就绪**（学生两问独立答对 ✅）
- **陷阱**：阻塞→运行不存在（必须先回就绪）；就绪→阻塞不存在
- **口诀**：调度就绪跑，片完回队等；等 I/O 进阻塞，醒来先排队，不能插队跑

### 块2 进程 vs 线程 ✅（引导后过，错题本 id41）
- **验证题（固定）**：正确说法 → B"共享全局变量，各自栈和寄存器"（学生首答 D 错——"线程崩溃不影响其他线程"✗，引导后改对）
- **陷阱**：A/D 镜像——"资源分配的基本单位"只能给进程；线程崩溃会连坐

### 块3 同步与互斥 ✅（错题本 id40）
- **算例（固定）**：S 初值 2，6P+3V → S=−1 → 1 个等待；S 初值 3，5P+2V → S=0、0 等待（学生数值全对 ✓）
- **错题实录**：辨识题"必须等上一道工序完成才能开始下一道"连续两次选"互斥/竞争"——正确=**同步**（接力赛型协作）
- **口诀**：P 申请减一，减完为负去排队；V 释放加一，加完非正就喊人

### 块4 PV 生产者消费者 ✅（断点重讲后通过，应试实录）
- **验证题（固定）**：单缓冲生产者序列 → 标准答案 **C**：P(empty)→P(mutex)→放→V(mutex)→V(full)（学生首答 A 死锁项、二答 B——B 的 V 顺序功能上可行但**考试按标准模板选 C**，应试实录已记）
- **口诀**：生产先看空，消费先看满，动窗口先上锁；先资源后互斥，顺序反了死锁见

## Day 7 · 操作系统（二）

### 块1 死锁四条件与处理策略 ✅
- **算例（固定）**："不属于四条件"→ 干扰项"可抢占"（反了，是不可剥夺）；"破坏条件属于避免"✗（属于预防）
- **验证题（固定）**：错误叙述选"破坏不可剥夺条件属于避免"→ C（学生独立答对 ✅）；四条件默写全对 ✅
- **口诀**：互斥请求不剥夺，循环等待死锁锁；一次性申请破保持，银行家边走边躲

### 块2 银行家算法 ✅
- **算例（固定）**：总10，P1(占3需5)/P2(占2需3)/P3(占2需4) → 剩3 → P2→回收→5 → P1→回收→8 → P3 → 安全序列 **P2→P1→P3**
- **验证题（固定）**：总8，P1(占2需4)/P2(占2需3)/P3(占2需2) → 安全，序列 P3→P2→P1（学生独立答对 ✅ 且自主发现序列不唯一）
- **陷阱**：不安全 ≠ 必死锁；安全序列不唯一

### 块3 分页与分段 ✅（重讲后结课：学生要求换讲法，改"装盒子+酒店民宿"类比后独立完成）
- **算例（固定）**：页大小 4KB、逻辑地址 8452、页号 2→块号 8 → 物理 = 8×4096+260 = 33028；快解：4KB=2¹² 低 12 位偏移
- **重讲有效讲法（记录）**：①拆换拼三步 + "装盒子"类比（能装满几盒=页号，剩几颗=偏移）+ "页 2→块 5 来自页表（OS 分配，不固定）"答疑 + 酒店标间(一维/分页) vs 民宿套房(二维/分段)类比
- **验证题**：1KB、2053、页 2→块 5 → 5125（学生三步独立 ✓）；概念：页内部碎片、段外部碎片
- **口诀**：一拆二换三拼回

### 块4 页面置换算法 ✅
- **算例（固定）**：2块、1,2,1,3,1 → FIFO 4、LRU 3（教学例，展示差别在第 4 步）；验证题：2块、2,3,1,2 → FIFO 4、LRU 4（学生两问独立答对 ✅）
- **陷阱**：FIFO 里重新调入的页变"最新"；LRU 每次访问刷新时间；不安全≠必死锁（银行家）
- **口诀**：先进先出排队走，最久不用 LRU 手，OPT 看未来，缺页次数它最少

## Day 4 · 计算机组成（四）

### 块1 奇偶校验 ✅
- **算例**：数据 1011010（4 个 1，偶数）；偶校验 → 校验位补 0 放最高位 → 码字 **01011010**
- **验证题**：偶校验生成 01011010；收到 00110100（3 个 1，奇数，违约）→ **能检测出** → 答案 C（学生首答 B 错在漏数"1"的个数，引导后答对 ⚠）
- **陷阱**：偶数个位同时出错检测不出；纠错不是它的功能

### 块2 海明码 ✅
- **算例（固定）**：数据 1011，k=3（2³=8≥4+3+1）；码字位置 1~7 = P1 P2 D1 P4 D2 D3 D4；P1 管 1,3,5,7 → 0；P2 管 2,3,6,7 → 1；P4 管 4,5,6,7 → 0；码字 **0110011**；纠错：各组校验结果拼二进制=出错位置号
- **"为什么"讲解（必须讲）**：位置号写二进制（身份证），P1=个位巡逻员管身份证个位为 1 的位置（1,3,5,7）；出错时报警组合 S4S2S1 恰好=出错位置的身份证号。演示：0110011 第 5 位出错 → P1、P4 报警 → 101=5；第 6 位出错 → P2、P4 报警 → 110=6（学生此题独立答对 ✅）
- **验证题**：信息位 8 位，纠 1 位错 → 2ᵏ≥8+k+1 → k=4 → 答案 **B**（学生答对但未写过程，追问后确认）
- **结课题（固定）**：数据 1100 → 码字 **0111100**（学生首答 0011100，P2 漏数位置 3 ❌ → 错题本 id 37）；变式：数据 1001 → **0011001**（学生独立答对 ✅）
- **陷阱**：位置 3 同时属于 P1+P2 组、位置 7 属于全部三组，清点时最容易漏

### 块3 CRC ✅
- **算例（固定）**：数据 10110，G(x)=x³+1（1001）→ r=3 → 10110 补 3 个 0 = 10110000 → 模 2 除法：10110000⊕1001(对齐位1)=00100000 → 窗口1000⊕1001=0001 → 余数 **100** → CRC 码 = **10110100**（自检：10110100÷1001 余 000）
- **验证题**：CRC 说法正确的是 → **生成多项式最高次幂为 3 时校验码 3 位**（干扰项：纠错是海明码功能、穿插是海明码摆位、校验码由余数确定不是商）→ 答案 C（学生独立答对 ✅）

### 块4 可靠性计算 ✅
- **公式**：串联 R=R₁×R₂×…；并联 R=1−(1−R₁)(1−R₂)…；混合从里往外（先并联小块再串联大块）；MTBF=1/λ
- **算例（固定）**：两个 0.9 串联=0.81；两个 0.9 并联=0.99；两个并联组（各两个 0.9）串联=0.99×0.99=0.9801
- **验证题**：两子系统串联（各由两个 0.9 并联）→ **0.9801**（学生答对 ✅）；变式：三个 0.9 并联 → 1−0.1³=**0.999**（学生答对 ✅）
