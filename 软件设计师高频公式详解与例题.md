# 软件设计师高频公式详解与例题

> 适用：计算机技术与软件专业技术资格（水平）考试，中级软件设计师。
>
> 使用方法：先读“含义”，再看“什么时候用”，然后遮住解析独立计算，最后核对易错点。公式只有和题目、单位一起记忆才有用。

## 零基础先记住这些单位和符号

| 写法 | 白话含义 |
|---|---|
| bit / Byte | 1 Byte = 8 bit；文件大小常用 Byte，网络速率常用 bit/s |
| Hz | 每秒发生多少次；MHz=\(10^6\) Hz，GHz=\(10^9\) Hz |
| ns / ms / s | 纳秒 / 毫秒 / 秒；\(1s=10^3ms=10^9ns\) |
| \(\lfloor x\rfloor\) | 向下取整，例如 \(\lfloor2.9\rfloor=2\) |
| \(\lceil x\rceil\) | 向上取整，例如 \(\lceil2.1\rceil=3\) |
| mod | 求余数，例如 \(10\bmod3=1\) |
| \(\log_2N\) | 2的多少次方等于N，例如 \(\log_2 8=3\) |
| 95% | 代入公式时写成0.95 |

---

## 一、数据表示与校验

### 题型识别

- 看到“某进制转十进制” -> 按权展开。
- 看到“n位补码、范围、溢出” -> 补码范围。
- 看到“阶码、尾数、对阶” -> 浮点数。
- 看到“至少多少校验位、纠正一位错” -> 海明码。
- 看到“生成多项式、模2除法、余数” -> CRC。

### 1. 按权展开与进制转换

#### 含义

就像十进制里"百位上的3表示300"一样，任意进制数的每一位都代表"数字 × 该位的权值"，把所有位的值加起来就是总数。比如二进制 101 中，从右到左权值依次是 1、2、4，所以 101 = 4+0+1 = 5。小数点左侧最低位权值为 \(r^0\)，右侧第一位为 \(r^{-1}\)。

#### 公式

\[
(a_na_{n-1}\cdots a_0.a_{-1}\cdots)_r=\sum_i a_i r^i
\]

- \(r\)：进制基数，如二进制的 \(r=2\)。
- \(a_i\)：第 \(i\) 位数字。
- 小数点左侧最低位权值为 \(r^0\)，右侧第一位为 \(r^{-1}\)。

#### 例题 1

将二进制数 \((101101.01)_2\) 转成十进制。

#### 解析

\[
1\times2^5+0\times2^4+1\times2^3+1\times2^2+0\times2^1+1\times2^0
+0\times2^{-1}+1\times2^{-2}
\]

\[
=32+8+4+1+0.25=45.25
\]

**答案：** \((45.25)_{10}\)。

#### 易错点

- 二进制小数位权依次是 \(2^{-1},2^{-2},2^{-3}\)，不是负数。
- 十六进制中的 A～F 分别表示 10～15。
- 二进制转八进制从小数点向两边每 3 位一组；转十六进制每 4 位一组。

---

### 2. 原码、反码、补码的表示范围

#### 含义

计算机用固定的二进制位（比如8位）来存数字，最左边一位是符号位（0表示正数，1表示负数）。补码是计算机存负数的方式，好处是加减法可以用同一套电路处理，而且0只有一种表示（不像原码有+0和-0）。

#### 公式

对于 \(n\) 位二进制数：

| 表示方法 | 表示范围 |
|---|---|
| 无符号数 | \(0\sim 2^n-1\) |
| 原码、反码 | \(-(2^{n-1}-1)\sim 2^{n-1}-1\) |
| 补码 | \(-2^{n-1}\sim 2^{n-1}-1\) |

负数补码求法：写出绝对值的二进制，按位取反，再加 1，符号位包含在固定字长内一起处理。

#### 例题 2

8 位补码可以表示的十进制整数范围是多少？十进制 `-18` 的 8 位补码是什么？

#### 解析

范围：

\[
-2^{7}\sim 2^7-1=-128\sim127
\]

求 `-18`：

1. `+18` 的 8 位二进制为 `00010010`。
2. 按位取反得 `11101101`。
3. 加 1 得 `11101110`。

**答案：** 范围为 `-128～127`；`-18` 的补码为 `11101110`。

#### 易错点

- 补码的负数一侧多一个值，8 位补码没有 `+128`，但有 `-128`。
- 必须先确定字长。同一个负数在 8 位和 16 位下的补码不同。
- 原码和反码有 `+0`、`-0`；补码只有一个零。

---

### 3. 浮点数

#### 含义

就像科学计数法把数字写成 \(1.23\times10^4\) 一样，浮点数把数写成 2的阶码次方 × 尾数。阶码决定"能表示多大或多小的数"（范围），尾数决定"能精确到小数点后几位"（精度）。

#### 公式

\[
N=2^E\times F
\]

- \(E\)：阶码，主要决定表示范围。
- \(F\)：尾数，主要决定精度。

二进制定点纯小数的常见规格化条件为：

\[
\frac{1}{2}\le |F|<1
\]

浮点加减步骤：**对阶 → 尾数运算 → 规格化 → 舍入 → 判断溢出**。

#### 例题 3

两个浮点数分别为 \(N_1=2^5\times0.1011_2\)，\(N_2=2^3\times0.1100_2\)。相加前应如何对阶？

#### 解析

阶码分别为 5 和 3。采用小阶向大阶对齐，将 \(N_2\) 的阶码增加 2，同时尾数右移 2 位：

\[
N_2=2^5\times0.001100_2
\]

然后再计算：

\[
N_1+N_2=2^5\times(0.101100+0.001100)_2
=2^5\times0.111000_2
\]

**答案：** 将 \(N_2\) 对齐到阶码 5，结果为 \(2^5\times0.111_2\)。

#### 易错点

- 小阶向大阶对齐，小阶数的尾数右移，可能损失低位精度。
- 规格化范围上界是开区间，不能写成 \(|F|\le1\)。
- 尾数位数决定精度，阶码位数决定范围。

---

### 4. 海明码

#### 含义

海明码是一种能发现并纠正一位错误的编码。原理是：在数据中间插入几个“校验位”（额外的检查位），每个校验位负责检查特定位置。如果某一位出错，校验结果会告诉你错在哪。就像快递单上加了好几个验证码，任何一个码不对就能定位问题。

#### 公式

数据位数为 \(m\)，校验位数为 \(r\)，要纠正一位错误，至少满足：

\[
2^r\ge m+r+1
\]

右侧的 `+1` 表示“没有错误”也需要一种状态。

码距与检错纠错能力：

- 检测 \(t\) 位错误：\(d_{min}\ge t+1\)。
- 纠正 \(t\) 位错误：\(d_{min}\ge2t+1\)。

#### 例题 4

传送 16 位数据，至少需要多少个海明校验位才能纠正一位错误？

#### 解析

逐个尝试：

- \(r=4\)：\(2^4=16<16+4+1=21\)，不满足。
- \(r=5\)：\(2^5=32\ge16+5+1=22\)，满足。

**答案：** 至少 5 位。

#### 易错点

- `2^r` 要覆盖数据位、校验位和无错状态。
- 普通可纠正一位错误的海明码最小码距是 3，不是 2。
- 校验位通常放在编号为 \(1,2,4,8,\ldots\) 的位置。

---

### 5. CRC 循环冗余校验

#### 含义

CRC 是一种查错方法。发送方和接收方约定一个“除数”（生成多项式），发送方用数据对它做一种特殊的除法（模2除法，就是异或运算，不借位不进位），把余数附在数据后面发出去。接收方用同样的方法除一次，余数为0说明数据没坏。就像身份证最后一位校验码的原理。

#### 规则

若生成多项式 \(G(x)\) 的最高次数为 \(r\)：

1. 在原数据后补 \(r\) 个 0。
2. 用生成多项式对应的二进制数作模 2 除法。
3. 把得到的 \(r\) 位余数放到原数据后。

模 2 除法的减法就是按位异或，不借位、不进位。

#### 例题 5

数据为 `1101`，生成多项式对应二进制为 `1011`，求 CRC 余数。

#### 解析

`1011` 的最高次数为 3，因此在数据后补 3 个 0：`1101000`。

进行模 2 除法：

```text
1101 XOR 1011 = 0110，落下一位得到 1100
1100 XOR 1011 = 0111，落下一位得到 1110
1110 XOR 1011 = 0101，落下一位得到 1010
1010 XOR 1011 = 0001
```

保留 3 位余数为 `001`。

**答案：** CRC 余数为 `001`，发送码字为 `1101001`。

#### 易错点

- 校验位数等于生成多项式的次数，不等于二进制串的长度。
- 每次运算使用异或，不使用普通十进制除法。

---

## 二、计算机组成原理

### 题型识别

- 看到“地址线、地址范围、小芯片拼大存储器” -> 存储容量与芯片组成。
- 看到“命中率、平均访问时间” -> 先画 Cache 命中/未命中路径。
- 看到“连续执行n条、流水段、吞吐率、加速比” -> 流水线。
- 看到“指令数、CPI、主频、MIPS” -> CPU执行时间。
- 看到“rpm、寻道、旋转等待” -> 磁盘访问时间。
- 看到“总线宽度、频率、带宽” -> 总线带宽。

### 6. 存储容量与芯片组成

#### 含义

地址线就像门牌号的位数--n根地址线能编出 \(2^n\) 个不同地址，也就是能访问 \(2^n\) 个存储单元。用小芯片拼大存储器时，“字扩展”是增加存储单元数量（不够多就多拼几片），“位扩展”是增加每个单元的位数（不够宽就并排拼），两个倍数相乘就是总片数。

#### 公式

\[
n\text{ 根地址线可访问 }2^n\text{ 个存储单元}
\]

\[
容量=存储单元数\times每单元位数
\]

由小芯片组成大存储器时：

\[
芯片数=\left\lceil\frac{目标字数}{芯片字数}\right\rceil
\times
\left\lceil\frac{目标字长}{芯片位数}\right\rceil
\]

前一项是字扩展，后一项是位扩展。

#### 例题 6

用 `16K×4 bit` 的芯片组成 `64K×16 bit` 的存储器，需要多少片？

#### 解析

字扩展倍数：

\[
64K/16K=4
\]

位扩展倍数：

\[
16/4=4
\]

总芯片数：

\[
4\times4=16
\]

**答案：** 16 片。

#### 例题 7

按字节编址的地址范围为 `4000H～47FFH`，容量是多少？

#### 解析

地址数量必须包含首尾地址：

\[
47FF_H-4000_H+1=800_H=2048
\]

按字节编址，所以容量为 2048 B，即 2 KB。

**答案：** 2 KB。

#### 易错点

- 地址范围求容量一定要 `末地址 - 首地址 + 1`。
- `B` 是字节，`b` 是位，\(1B=8b\)。
- 先看按字节、按字还是按位编址。

---

### 7. Cache 平均访问时间

#### 含义

Cache（高速缓存）是CPU和主存之间的“小仓库”，存着最近常用的数据。CPU找数据时先查Cache，找到了叫“命中”（快），没找到叫“未命中”（要去慢腾腾的主存里取）。命中率越高，平均访问速度越快。

#### 公式

最稳妥的方法是先画访问路径。每次都先查询 Cache，若未命中，再额外访问主存：

\[
T_{avg}=hT_c+(1-h)(T_c+T_m)=T_c+(1-h)T_m
\]

- \(h\)：Cache 命中率。
- \(T_c\)：每次查询 Cache 的时间。
- \(T_m\)：未命中后额外访问主存的时间。

若题目直接给出“未命中路径从开始到结束的总耗时” \(T_{miss}\)，则使用：

\[
T_{avg}=hT_c+(1-h)T_{miss}
\]

#### 例题 8

Cache 查询时间为 10 ns，未命中后访问主存还需 100 ns，命中率为 95%，求平均访问时间。

#### 解析

先分两条路径：

- 命中：只需 10 ns，概率为 0.95。
- 未命中：先查 Cache 再访问主存，共 \(10+100=110\) ns，概率为 0.05。

\[
T_{avg}=0.95\times10+0.05\times110=15\text{ ns}
\]

**答案：** 15 ns。

#### 易错点

- `95%` 要写成 `0.95`。
- 先判断主存时间是完整时间还是额外失效代价，避免重复计算 Cache 查询时间。
- 直接映射冲突最多，全相联冲突最少，但硬件成本最高。

---

### 8. 流水线

#### 含义

流水线就像工厂流水线——把一条指令的执行拆成几个阶段（比如取指、分析、执行），每个阶段同时处理不同的指令。就像洗衣的“洗->烘->叠”三阶段，第一批在烘的时候第二批已经在洗了。流水线的速度取决于最慢的那个阶段。

#### 公式

流水周期：

\[
\Delta t=\max(t_1,t_2,\ldots,t_k)
\]

执行 \(n\) 条指令的严格公式：

\[
T_{pipe}=\sum_{i=1}^{k}t_i+(n-1)\Delta t
\]

若每段都按统一周期计算：

\[
T_{pipe}=(k+n-1)\Delta t
\]

顺序执行时间、加速比和吞吐率：

\[
T_{seq}=n\sum t_i
\]

\[
S=\frac{T_{seq}}{T_{pipe}}
\]

\[
TP=\frac{n}{T_{pipe}},\qquad TP_{max}\to\frac{1}{\Delta t}
\]

#### 例题 9

某指令分取指、分析、执行三个阶段，时间分别为 2 ns、3 ns、4 ns。连续执行 10 条指令，求流水执行时间、顺序执行时间和加速比。

#### 解析

流水周期：

\[
\Delta t=\max(2,3,4)=4\text{ ns}
\]

流水时间：

\[
T_{pipe}=2+3+4+(10-1)\times4=45\text{ ns}
\]

顺序时间：

\[
T_{seq}=10\times(2+3+4)=90\text{ ns}
\]

加速比：

\[
S=90/45=2
\]

**答案：** 流水时间 45 ns，顺序时间 90 ns，加速比 2。

#### 易错点

- 第一条指令要完整经过全部阶段，不能直接写 \(n\Delta t\)。
- \(1/\Delta t\) 是指令数量很大时的最大吞吐率。
- 阶段时间不等时，严格公式与统一周期公式结果可能不同，应按题目口径选择。

---

### 9. CPU 执行时间

#### 含义

CPU执行一个程序需要多久，取决于三件事：有多少条指令（指令条数）、每条指令平均要几个时钟周期（CPI）、CPU每秒能发出多少个时钟周期（主频）。

#### 公式

\[
CPU时间=指令条数\times CPI\times时钟周期
\]

又因为 \(时钟周期=1/主频\)：

\[
CPU时间=\frac{指令条数\times CPI}{主频}
\]

CPI 表示平均每条指令需要的时钟周期数。MIPS 只适合在指令集和工作负载相近时辅助比较，不能单独代表 CPU 的真实性能。

#### 例题 10

某程序执行 2 亿条指令，平均 CPI 为 1.5，CPU 主频为 3 GHz，求 CPU 执行时间。

#### 解析

\[
T=\frac{2\times10^8\times1.5}{3\times10^9}=0.1\text{ s}
\]

**答案：** 0.1 秒。

#### 易错点

- GHz 是 \(10^9\) Hz，MHz 是 \(10^6\) Hz。
- 主频高不必然更快，还要看指令数和 CPI。

---

### 10. 磁盘容量与访问时间

#### 含义

磁盘读写数据分三步：①磁头移动到正确的磁道（寻道，像电梯找楼层），②等磁盘转到正确的位置（旋转等待，像等传送带上的包裹转到面前），③读取数据（传输）。磁盘转速越快，等的时间越短。

#### 公式

\[
磁盘容量=盘面数\times每面磁道数\times每磁道扇区数\times每扇区字节数
\]

\[
访问时间=寻道时间+旋转等待时间+数据传输时间
\]

转速为 \(r\) rpm 时：

\[
一圈时间=\frac{60}{r}\text{ s},\qquad 平均旋转等待时间=\frac{30}{r}\text{ s}
\]

#### 例题 11

某磁盘转速为 7200 rpm，平均寻道时间 5 ms，读取一个扇区的传输时间为 0.1 ms，求平均访问时间。

#### 解析

平均旋转等待时间：

\[
\frac{30}{7200}\text{ s}=0.004167\text{ s}=4.167\text{ ms}
\]

平均访问时间：

\[
5+4.167+0.1=9.267\text{ ms}
\]

**答案：** 约 9.27 ms。

#### 易错点

- `rpm` 是每分钟转数，要换算成秒。
- 平均旋转等待通常按半圈计算。
- 盘片数不一定等于有效盘面数，以题目说明为准。

---

### 11. MIPS 与总线带宽

#### 含义

MIPS就是“CPU每秒能执行多少百万条指令”，数值越大通常表示执行指令越快。总线是计算机各部件之间传数据的“高速公路”：总线宽度相当于车道数（一次能传几bit），总线频率相当于每秒能跑几趟。两者相乘并把bit换成Byte，就是每秒能搬多少数据。

#### 公式

\[
MIPS=\frac{主频}{CPI\times10^6}
\]

或由执行时间反推：

\[
MIPS=\frac{指令条数}{执行时间\times10^6}
\]

总线带宽：

\[
总线带宽=\frac{总线宽度(bit)\times总线频率(Hz)}{8}\quad(B/s)
\]

若总线频率以 MHz 为单位：

\[
带宽(GB/s)=\frac{总线宽度\times频率(MHz)}{8\times10^3}
\]

#### 例题 10b

某 CPU 主频为 2 GHz，平均 CPI 为 1.25，求 MIPS。

#### 解析

\[
MIPS=\frac{2\times10^9}{1.25\times10^6}=1600
\]

**答案：** 1600 MIPS。

#### 例题 10c

某总线宽度为 64 bit，总线频率为 800 MHz，求总线带宽。

#### 解析

\[
带宽=\frac{64\times800\times10^6}{8}=6.4\times10^9\text{ B/s}=6.4\text{ GB/s}
\]

**答案：** 6.4 GB/s。

#### 易错点

- 总线宽度以 bit 为单位，结果要除以 8 转为字节。
- MIPS 与 CPI 成反比，CPI 越小 MIPS 越大。
- 总线频率可能需要区分单沿/双沿传输（DDR 方式下有效频率翻倍）。

---

### 12. 波特率与比特率

#### 含义

波特率是每秒传输多少个码元（一个码元可以理解为一个“信号符号”，不等同于信号变化次数）。比特率是每秒传输多少个二进制位。如果一个码元有4种可能状态，它就能表示 \(\log_2 4=2\) bit 信息。

#### 公式

\[
比特率=波特率\times\log_2 N
\]

- \(N\)：每个码元可取的离散状态数。
- 曼彻斯特编码中每个码元只表示 1 bit，且时钟频率是数据速率的 2 倍。

#### 例题 10d

某信道波特率为 1200 Baud，每个码元有 4 种状态，求比特率。

#### 解析

\[
比特率=1200\times\log_2 4=1200\times2=2400\text{ bit/s}
\]

**答案：** 2400 bit/s。

#### 易错点

- 波特率不等于比特率，除非 \(N=2\)（每个码元只表示 1 bit）。
- 曼彻斯特编码效率为 50%，即数据速率只有信号速率的一半。

---

## 三、操作系统

### 题型识别

- 看到“到达、服务、完成时间” -> 先画调度甘特图，再算周转时间。
- 看到“页大小、逻辑地址、物理块号” -> 分页地址转换。
- 看到“访问序列、页框、FIFO/LRU/OPT” -> 列表模拟页面置换。
- 看到“同类资源、每进程最大需求、保证不死锁” -> 死锁资源下限。
- 看到“位示图、块号、第几字第几位” -> 位示图换算。

### 13. 进程调度指标

#### 含义

周转时间就是“从提交到完成总共等了多久”。带权周转时间衡量“等待是否值得”--如果服务时间很短却等了很久，带权周转就很大（不划算）。HRRN（高响应比）会优先调度“等了很久且服务时间不太长”的进程。

#### 公式

\[
周转时间=完成时间-到达时间
\]

\[
带权周转时间=\frac{周转时间}{服务时间}
\]

\[
等待时间=周转时间-服务时间
\]

高响应比优先 HRRN 的响应比：

\[
响应比=\frac{等待时间+服务时间}{服务时间}
=1+\frac{等待时间}{服务时间}
\]

#### 例题 12

三个进程同时到达，服务时间分别为 P1=6、P2=2、P3=4。采用非抢占式短作业优先 SJF，求执行顺序和平均周转时间。

#### 解析

全部同时到达，按服务时间从短到长执行：P2 → P3 → P1。

| 进程 | 完成时间 | 周转时间 |
|---|---:|---:|
| P2 | 2 | 2 |
| P3 | 6 | 6 |
| P1 | 12 | 12 |

\[
平均周转时间=(2+6+12)/3=20/3\approx6.67
\]

**答案：** 顺序为 P2 → P3 → P1，平均周转时间约 6.67。

#### 易错点

- 必须先确认是否抢占以及各进程到达时间。
- 周转时间从到达开始计算，不是从首次获得 CPU 开始。
- HRRN 每完成一个进程后，要对当前已到达进程重新计算响应比。

---

### 14. 分页地址转换

#### 含义

内存被分成固定大小的“页”（比如每页1KB），程序也被同样分页。逻辑地址就像“第几页、第几行”，通过页表查到这一页实际放在内存的哪个物理块，再拼出真实地址。就像书的目录告诉你“第3章在第45页”。

#### 公式

页大小为 \(P\)，逻辑地址为 \(A\)：

\[
页号=\left\lfloor\frac{A}{P}\right\rfloor
\]

\[
页内偏移=A\bmod P
\]

若页表给出的页框号为 \(f\)：

\[
物理地址=f\times P+页内偏移
\]

#### 例题 13

页面大小为 1 KB，逻辑地址为 2500，页表显示逻辑页 2 装入物理块 7。求物理地址。

#### 解析

\(1KB=1024B\)。

\[
页号=\lfloor2500/1024\rfloor=2
\]

\[
偏移=2500-2\times1024=452
\]

\[
物理地址=7\times1024+452=7620
\]

**答案：** 7620。

#### 易错点

- 页号一般从 0 开始。
- 页大小必须先统一为字节。
- 页号越界与缺页中断不是同一件事：前者地址非法，后者页面尚未调入内存。

---

### 15. 页面置换

#### 含义

内存页框装满后需要淘汰页面，就像书架放满了要拿走一本书才能放新的：

- FIFO：淘汰最早进入内存的页面。
- LRU：淘汰最长时间没有被访问的页面。
- OPT：淘汰未来最晚才会再次使用的页面。

#### 例题 14

有 3 个空页框，页面访问序列为 `1, 2, 3, 1, 4, 2`，使用 FIFO，缺页多少次？

#### 解析

| 访问 | 页框状态 | 是否缺页 |
|---|---|---|
| 1 | 1 | 是 |
| 2 | 1,2 | 是 |
| 3 | 1,2,3 | 是 |
| 1 | 1,2,3 | 否 |
| 4 | 4,2,3 | 是，淘汰最早进入的 1 |
| 2 | 4,2,3 | 否 |

**答案：** 缺页 4 次。

#### 易错点

- 初始空页框装入页面也算缺页。
- FIFO 命中页面时不改变其进入内存的先后顺序。
- FIFO 可能出现页框增多反而缺页更多的 Belady 异常；LRU、OPT 不会。

---

### 16. 死锁资源数

#### 含义

死锁就是“大家都占着资源不放手，又都在等对方释放”的僵局。保证不死锁的最少资源数：假设最坏情况--每个进程都拿到了“差一个就够”的资源量，此时只要再多给1个，就有一个进程能跑完并释放资源，打破僵局。

#### 公式

有 \(n\) 个进程，每个进程最多需要 \(k\) 个同类资源，为保证一定不会死锁，资源数至少为：

\[
m_{min}=n(k-1)+1
\]

#### 例题 15

系统有 5 个进程，每个进程最多需要 3 台同类设备。至少配置多少台设备才能保证不发生死锁？

#### 解析

最坏情况下，每个进程都已得到 \(k-1=2\) 台，并且都还差 1 台：共占用 \(5\times2=10\) 台。只要再多 1 台，就能让一个进程完成并释放资源。

\[
m_{min}=5\times(3-1)+1=11
\]

**答案：** 11 台。

#### 易错点

- 该公式给出“保证不死锁”的资源下限，不是判断当前状态是否已经死锁。
- 仅适用于每个进程最大需求相同、资源同类等特定题设。

---

### 17. 位示图

#### 含义

位示图就像停车场的车位显示屏--每个二进制位对应一个磁盘块，0表示空闲（车位空着），1表示已分配（车位停了车）。通过块号除以每字位数，可以快速算出它落在哪个字、哪一位。

#### 公式

若一个字含 \(w\) 个二进制位，第 \(i\) 个字从 1 开始编号、物理块从 0 开始编号，则：

\[
字号=\left\lfloor\frac{块号}{w}\right\rfloor+1
\]

\[
位号=块号\bmod w
\]

#### 例题 16

位示图每个字 32 位，物理块号为 70，它对应第几个字、第几位？位从 0 开始编号。

#### 解析

\[
字号=\lfloor70/32\rfloor+1=3
\]

\[
位号=70\bmod32=6
\]

**答案：** 第 3 个字的第 6 位。

#### 易错点

- 先确认字号和位号从 0 还是从 1 开始，考试题会明确口径。

---

## 四、计算机网络

### 题型识别

- 看到“文件大小、链路速率、距离” -> 发送/传播时延。
- 看到“/前缀、掩码、主机数、广播地址” -> 子网划分。
- 无噪声且给码元状态数 -> 奈奎斯特；给信噪比 -> 香农；都给则取较小值。
- 看到“CSMA/CD、传播距离、最小帧” -> 最小帧长。

### 18. 网络时延

#### 含义

发送时延是“把数据全部推上网络线缆”的时间（像把水灌进水管），传播时延是“一个比特从这端跑到那端”的时间（像水从管子一头流到另一头）。总时延还要加上路由器处理和排队等待的时间。

#### 公式

\[
发送时延=\frac{数据长度(bit)}{发送速率(bit/s)}
\]

\[
传播时延=\frac{链路距离(m)}{传播速度(m/s)}
\]

\[
总时延=发送时延+传播时延+处理时延+排队时延
\]

发送时延是“把所有比特推上链路所需时间”；传播时延是“一个比特在链路上行进所需时间”。

#### 例题 17

在 100 Mbps 链路上传输一个 10 MB 文件，忽略传播、排队和处理时延，至少需要多长时间？按 \(1MB=10^6B\) 计算。

#### 解析

先把字节换成比特：

\[
10MB=10\times10^6\times8=80\times10^6bit
\]

\[
T=\frac{80\times10^6}{100\times10^6}=0.8s
\]

**答案：** 0.8 秒。

#### 易错点

- 文件大小常用 B，传输速率常用 bit/s，必须乘 8。
- Mbps 通常采用十进制 \(10^6\)，除非题目另有说明。
- 多段存储转发链路可能需要在每一段重新计算发送时延。

---

### 19. 子网划分

#### 含义

IP地址有32位，斜杠后面的数字p表示前p位是网络号，剩下的32-p位是主机号。主机号全0是网络地址（代表整个网络），全1是广播地址（发给所有主机），这两个不能分配给具体机器，所以可用主机数要减2。

#### 公式

IPv4 前缀长度为 `/p`：

\[
主机位数h=32-p
\]

\[
地址总数=2^h
\]

传统普通子网可用主机数：

\[
2^h-2
\]

网络地址为 `IP AND 子网掩码`；广播地址是把主机位全部置 1。

#### 例题 18

网络 `192.168.10.0/26` 有多少个地址、多少个传统可用主机地址？第一个子网的广播地址是什么？

#### 解析

主机位数：

\[
h=32-26=6
\]

地址总数：

\[
2^6=64
\]

传统可用主机数：

\[
64-2=62
\]

一个子网跨 64 个地址，范围为 `192.168.10.0～192.168.10.63`，所以广播地址为 `192.168.10.63`。

**答案：** 64 个地址，62 个传统可用主机地址，广播地址为 `192.168.10.63`。

#### 易错点

- 减去的是主机号全 0 的网络地址和主机号全 1 的广播地址。
- `/31`、`/32` 有特殊用途，不机械套用 \(2^h-2\)。
- 子网步长可用 `256 - 掩码中变化的那个字节` 快速计算。

---

### 20. 奈奎斯特与香农公式

#### 含义

奈奎斯特公式回答“没有噪声干扰的理想条件下，信道每秒最多能传多少bit”。香农公式回答“有噪声的现实条件下，信道每秒最多能可靠传多少bit”。两个公式可以同时用，取较小值作为实际极限。

#### 公式

奈奎斯特：

\[
R=2W\log_2N
\]

- \(W\)：带宽，单位 Hz。
- \(N\)：每个码元可取的离散状态数。
- \(R\)：最大数据速率，单位 bit/s。

香农：

\[
C=W\log_2(1+S/N)
\]

分贝换算：

\[
SNR_{dB}=10\log_{10}(S/N)
\]

#### 例题 19

无噪声信道带宽为 3 kHz，每个码元有 16 种状态，最大数据速率是多少？

#### 解析

\[
R=2\times3000\times\log_2 16
=6000\times4=24000bit/s
\]

**答案：** 24 kbps。

#### 易错点

- 奈奎斯特的 \(N\) 是码元状态数，不是数据位数。
- 香农公式中的 \(S/N\) 是普通倍数。若给出 dB，要先转换。

---

## 五、软件工程与项目管理

### 题型识别

- 看到“控制流图、边数、节点数、判定节点” -> McCabe复杂度。
- 看到“乐观、最可能、悲观时间” -> PERT三点估算。
- 看到“网络图、最短工期、关键活动、时差” -> 关键路径正推和逆推。
- 看到“串联、并联、可靠度、MTTF/MTTR” -> 可靠性与可用性。
- 看到“n人团队、两两沟通” -> 沟通路径。
- 看到“KLOC、项目类型、人月” -> COCOMO。

### 21. McCabe 环路复杂度

#### 含义

McCabe 环路复杂度表示控制流图中“线性独立路径”的数量，也是基本路径测试通常至少要覆盖的独立路径数。它不等于程序所有可能执行路径的总数，因为循环可以产生无限多种执行次数。

#### 公式

对单连通控制流图：

\[
V(G)=E-N+2
\]

- \(E\)：控制流图的边数。
- \(N\)：控制流图的节点数。

对于每个判定均为二分支的结构化程序，还可用：

\[
V(G)=判定节点数+1
\]

也可以数控制流图区域数，外部区域也要计算。

#### 例题 20

某控制流图有 12 条边、9 个节点，求环路复杂度和至少需要设计的基本路径测试数。

#### 解析

\[
V(G)=12-9+2=5
\]

环路复杂度等于线性独立路径数，因此至少应设计 5 条基本路径。

**答案：** 环路复杂度为 5，至少 5 条基本路径。

#### 易错点

- \(E\)、\(N\) 是控制流图的边和节点，不是代码行数。
- 多分支 `switch` 不能不加分析地直接套“判定节点数 + 1”。

---

### 22. PERT 三点估算

#### 含义

当无法确定一个活动到底要多久时，给出三个估计：最乐观（一切顺利）、最可能（正常情况）、最悲观（各种意外）。加权平均得到期望时间，最可能时间的权重最大（4份），乐观和悲观各1份，共6份。

#### 公式

\[
t_e=\frac{a+4m+b}{6}
\]

\[
\sigma^2=\left(\frac{b-a}{6}\right)^2
\]

- \(a\)：最乐观时间。
- \(m\)：最可能时间。
- \(b\)：最悲观时间。

独立活动组成一条路径时，路径期望时间相加，路径方差也相加。

#### 例题 21

某活动最乐观需 2 天，最可能需 5 天，最悲观需 8 天。求期望时间和方差。

#### 解析

\[
t_e=\frac{2+4\times5+8}{6}=\frac{30}{6}=5\text{ 天}
\]

\[
\sigma^2=\left(\frac{8-2}{6}\right)^2=1
\]

**答案：** 期望时间 5 天，方差 1 天²。

#### 易错点

- 中间的最可能时间权重是 4。
- 方差要平方；标准差才是 \((b-a)/6\)。

---

### 23. 关键路径与时差

#### 含义

一个项目有多条并行的工作路径，其中耗时最长的那条叫“关键路径”--它决定了项目最快多久能完成。关键路径上的任何活动延期，整个项目就延期。非关键路径上的活动有一定缓冲时间（时差）。关键活动的总时差为 0。

#### 公式与规则

- 正向计算最早时刻：有多个前驱时取最大值。
- 反向计算最迟时刻：有多个后继时取最小值。
- 活动总时差：

\[
TF=LS-ES=LF-EF
\]

#### 例题 22

某项目有两条从开始到结束的路径：A-B-D 工期为 `3+5+4=12` 天，A-C-D 工期为 `3+2+4=9` 天。求关键路径和最短工期。

#### 解析

项目必须等待所有路径上的任务完成，因此由最长路径控制总工期。

**答案：** 关键路径是 A-B-D，项目最短工期为 12 天。A-C-D 比关键路径短 3 天；在这个简化网络且没有其他约束时，该支路最多可延误 3 天而不影响总工期。严格来说，总时差是活动的属性。

#### 易错点

- “最短工期”由网络图中的最长路径决定。
- 存在多条关键路径时，只缩短其中一条不一定能缩短总工期。
- 关键活动延误通常会直接影响总工期。

---

### 24. 系统可靠性与可用性

#### 含义

串联就像圣诞灯串--一个灯坏了整串都不亮，所以串联越多越不可靠。并联就像多条备用路线--只要有一条通着系统就能用，所以并联能提高可靠性。可用性还要考虑修复速度：修得越快，系统可用时间越长。

#### 公式

串联系统中任何一个部件失败都会导致系统失败：

\[
R_s=\prod_{i=1}^{n}R_i
\]

并联系统只要至少一个部件正常，系统就正常：

\[
R_p=1-\prod_{i=1}^{n}(1-R_i)
\]

稳态可用性常用：

\[
A=\frac{MTTF}{MTTF+MTTR}
\]

- MTTF：平均无故障时间。
- MTTR：平均修复时间。

#### 例题 23

两个可靠度均为 0.9 的部件组成系统，分别求串联和并联时的可靠度。

#### 解析

串联：

\[
R_s=0.9\times0.9=0.81
\]

并联：

\[
R_p=1-(1-0.9)^2=1-0.01=0.99
\]

**答案：** 串联可靠度 0.81，并联可靠度 0.99。

#### 易错点

- 并联可靠度不是简单相加，应先求“全部失败”的概率。
- 串联系统部件越多，系统可靠度通常越低；并联冗余可提高可靠度。

---

### 25. 沟通路径

#### 含义

如果团队中每两个人都要直接沟通，就要为每一对人建立一条沟通路径，所以人数增加后沟通成本会很快上升。主程序员制让其他人只和主程序员沟通，就像星形网络，可把路径数降为 \(n-1\)。

#### 公式

\(n\) 人彼此直接沟通时，沟通路径数为：

\[
C=\frac{n(n-1)}{2}
\]

采用主程序员制，其他人只与主程序员沟通时：

\[
C=n-1
\]

#### 例题 24

一个 8 人团队中每两人都可直接沟通，共有多少条沟通路径？

#### 解析

\[
C=\frac{8\times7}{2}=28
\]

**答案：** 28 条。

---

## 六、数据结构与算法

### 题型识别

- 看到代码循环或要求时间复杂度 -> 逐层数循环次数。
- 看到“叶子节点、度为2、满二叉树” -> 二叉树节点关系。
- 看到“完全二叉树、节点编号、深度” -> 完全二叉树性质。
- 看到“哈希函数、线性探测、ASL” -> 逐个统计比较次数。

### 26. 常见复杂度增长顺序

\[
O(1)<O(\log n)<O(n)<O(n\log n)<O(n^2)<O(2^n)<O(n!)
\]

复杂度描述“数据量翻倍时，算法耗时增长多快”。就像搬家：O(1)是无论多少东西都一趟搞定；O(log n)是二分查找--每猜一次排除一半；O(n)是把每个箱子都搬一次；O(n²)是每两个箱子都要互相比较一次。只看增长最快的那一项，忽略常数和低阶项。

#### 常见算法复杂度表

| 算法 | 最好 | 平均 | 最坏 | 备注 |
|---|---:|---:|---:|---|
| 顺序查找 | \(O(1)\) | \(O(n)\) | \(O(n)\) | 无需有序 |
| 二分查找 | \(O(1)\) | \(O(\log n)\) | \(O(\log n)\) | 要求有序且支持随机访问 |
| 冒泡排序 | \(O(n)\)* | \(O(n^2)\) | \(O(n^2)\) | *优化后最好为线性 |
| 直接插入排序 | \(O(n)\) | \(O(n^2)\) | \(O(n^2)\) | 基本有序时较好 |
| 选择排序 | \(O(n^2)\) | \(O(n^2)\) | \(O(n^2)\) | 比较次数稳定 |
| 快速排序 | \(O(n\log n)\) | \(O(n\log n)\) | \(O(n^2)\) | 平均很快 |
| 归并排序 | \(O(n\log n)\) | \(O(n\log n)\) | \(O(n\log n)\) | 需额外空间 |
| 堆排序 | \(O(n\log n)\) | \(O(n\log n)\) | \(O(n\log n)\) | 原地、不稳定 |

#### 例题 25

以下代码的时间复杂度是多少？

```c
for (i = 1; i < n; i *= 2) {
    for (j = 0; j < n; j++) {
        count++;
    }
}
```

#### 解析

外层循环每次把 `i` 乘 2，执行约 \(\log_2n\) 次；内层每次执行 \(n\) 次。嵌套循环次数相乘：

\[
T(n)=n\log_2n
\]

忽略对数底数：

**答案：** \(O(n\log n)\)。

#### 易错点

- 顺序代码复杂度相加后取最高阶；嵌套循环通常相乘。
- 每次规模减半或变量翻倍，通常出现 \(\log n\)。
- 快速排序平均为 \(O(n\log n)\)，最坏为 \(O(n^2)\)。

---

### 27. 二叉树节点关系

#### 含义

一棵二叉树中，“末端节点”（叶子，没有孩子）永远比“有两个孩子的节点”多1个。原因：每个有2个孩子的节点贡献2个“孩子位”，其中1个用于延伸树干，剩下的才成为叶子。记住结论 \(n_0=n_2+1\) 即可快速做题。

#### 公式

任何非空二叉树中，叶子节点数 \(n_0\) 与度为 2 的节点数 \(n_2\) 满足：

\[
n_0=n_2+1
\]

深度为 \(h\) 的满二叉树节点数：

\[
2^h-1
\]

第 \(i\) 层最多有：

\[
2^{i-1}
\]

#### 例题 26

某二叉树中度为 2 的节点有 15 个，叶子节点有多少个？

#### 解析

\[
n_0=n_2+1=15+1=16
\]

**答案：** 16 个。

#### 易错点

- 根节点位于第 1 层时，第 \(i\) 层最多为 \(2^{i-1}\)。
- \(n_0=n_2+1\) 与度为 1 的节点数量无关。

---

### 28. 完全二叉树性质

#### 含义

完全二叉树像从上到下、从左到右排座位：除最后一层外每层都坐满，最后一层也必须从最左边连续入座。按从1开始的顺序给节点编号后，父子编号之间会形成固定的2倍关系，因此可以不用画树就找到父节点和孩子。

#### 公式

有 \(n\) 个节点的完全二叉树，深度为：

\[
h=\lfloor\log_2 n\rfloor+1
\]

编号为 \(i\) 的节点（从 1 开始），其左孩子编号为 \(2i\)，右孩子编号为 \(2i+1\)，父节点编号为 \(\lfloor i/2\rfloor\)。

#### 例题 27b

一棵完全二叉树有 500 个节点，它的深度是多少？

#### 解析

\[
h=\lfloor\log_2 500\rfloor+1=\lfloor8.966\rfloor+1=8+1=9
\]

**答案：** 深度为 9。

#### 易错点

- 深度从 1 开始计数。
- 完全二叉树第 1 到 \(h-1\) 层是满的，第 \(h\) 层从左到右连续排列。

---

## 四（补充）、计算机网络公式

### 29. IP 地址分类

#### 含义

IP地址按首字节范围分成几类，就像电话号码按区号分类。A类网络数量少，但每个网络能容纳很多主机，适合大型网络；C类网络数量多，但每个网络容纳的主机少，适合小型网络。考试时看第一个十进制数字就能快速判断类别。

#### 公式

| 类别 | 首字节范围 | 网络号位数 | 主机号位数 | 最大网络数 | 每网主机数 |
|---|---|---:|---:|---:|---:|
| A | 1～126 | 8 | 24 | \(2^7-2=126\) | \(2^{24}-2\) |
| B | 128～191 | 16 | 16 | \(2^{14}\) | \(2^{16}-2\) |
| C | 192～223 | 24 | 8 | \(2^{21}\) | \(2^8-2=254\) |

A 类地址以 `0` 开头，B 类以 `10` 开头，C 类以 `110` 开头。

#### 例题 27c

IP 地址 `172.16.5.3` 属于哪一类？该网络最多能有多少台主机？

#### 解析

首字节 `172` 在 128～191 范围内，属于 B 类。B 类主机号 16 位：

\[
2^{16}-2=65534
\]

**答案：** B 类，最多 65534 台主机。

#### 易错点

- A 类网络数减 2 是因为全 0 和 127（环回）保留。
- 主机号全 0 是网络地址，全 1 是广播地址，都要减去。
- D 类（224～239）用于组播，E 类（240～255）保留。

---

### 30. CSMA/CD 最小帧长

#### 含义

CSMA/CD 是传统共享式以太网的冲突检测规则。如果两台机器同时发送，信号会“撞车”。为保证发送方在一帧发完前还能听到最远处返回的冲突信号，帧不能太短，所以最小帧长等于网络速率乘以信号往返时间。

#### 公式

\[
最小帧长=2\times带宽\times单程传播时延
\]

或：

\[
L_{min}=2\times R\times\frac{d}{v}
\]

- \(R\)：网络带宽（bit/s）。
- \(d\)：单程链路长度（m）。
- \(v\)：信号传播速度，约 \(2\times10^8\) m/s。

#### 例题 27d

某以太网带宽为 100 Mbps，最远两站距离为 1000 m，信号传播速度为 \(2\times10^8\) m/s，求最小帧长。

#### 解析

单程传播时延：

\[
t=\frac{1000}{2\times10^8}=5\times10^{-6}\text{ s}
\]

最小帧长：

\[
L_{min}=2\times100\times10^6\times5\times10^{-6}=1000\text{ bit}=125\text{ B}
\]

**答案：** 125 字节。

#### 易错点

- 公式中的 2 倍是因为冲突信号需要往返。
- 带宽单位是 bit/s，结果除以 8 才是字节。
- 例题使用的是只考虑线缆传播的理想化模型；标准以太网 64 字节最小帧还包含中继器和设备延迟预算，不能只用 2500 m 线缆传播时间直接推出。

---

### 31. COCOMO 模型

#### 含义

COCOMO 用预计代码行数来估算项目需要多少工作量和时间。“人月”表示一个人工作一个月的工作量，例如12人月不一定是12个人做1个月，也可能是3个人做4个月。代码量越大，沟通和管理成本也越高，所以工作量通常不是简单成比例增长。

#### 公式

基本 COCOMO：

\[
E=a\times(KLOC)^b
\]

\[
D=c\times E^d
\]

- \(E\)：工作量（人月）。
- \(D\)：开发时间（月）。
- \(KLOC\)：千行代码数。
- \(a,b,c,d\)：模型参数，取决于项目类型。

基本 COCOMO 三种项目类型的参数：

| 类型 | \(a\) | \(b\) | \(c\) | \(d\) |
|---|---:|---:|---:|---:|
| 组织型（简单） | 2.4 | 1.05 | 2.5 | 0.38 |
| 半独立型 | 3.0 | 1.12 | 2.5 | 0.35 |
| 嵌入型（复杂） | 3.6 | 1.20 | 2.5 | 0.32 |

#### 例题 27e

某组织型项目估计代码量为 50 KLOC，求工作量和开发时间。

#### 解析

工作量：

\[
E=2.4\times50^{1.05}\approx2.4\times60.8\approx145.9\text{ 人月}
\]

开发时间：

\[
D=2.5\times145.9^{0.38}\approx16.6\text{ 月}
\]

**答案：** 约 145.9 人月，16.6 个月。

#### 易错点

- KLOC 是千行代码，不是行数。
- 指数 \(b>1\) 意味着工作量随规模超线性增长。
- 基本 COCOMO 精度有限，中级和详细 COCOMO 引入更多调整因子。

---

## 六（续）、数据结构与算法

### 32. 哈希表平均查找长度

#### 含义

哈希表像一排带编号的储物柜（用“关键字 mod 柜子数”算出该放哪个柜子）。如果目标柜子被占，就往后找下一个空柜（冲突处理）。ASL就是“平均找一个人要开几个柜子”--把每个关键字的比较次数加起来除以关键字总数。具体值依赖冲突处理方法和表中实际位置，不能只背一个通式。

#### 例题 27

使用线性探测法，散列函数为 \(H(k)=k\bmod7\)，依次插入关键字 `8, 15, 22`。求成功查找的平均查找长度。

#### 解析

- `8 mod 7 = 1`，放在位置 1，比较 1 次。
- `15 mod 7 = 1`，位置 1 冲突，探测位置 2，比较 2 次。
- `22 mod 7 = 1`，位置 1、2 冲突，放在位置 3，比较 3 次。

\[
ASL_{成功}=\frac{1+2+3}{3}=2
\]

**答案：** 2。

#### 易错点

- 成功查找和失败查找的 ASL 计算对象不同。
- 开放定址法发生冲突后，比较次数要包含最初散列位置。

---

## 七、综合练习

先独立完成，再看答案。

### 练习 1：补码

16 位补码能表示的整数范围是什么？

### 练习 2：海明码

传送 32 位数据，至少需要多少个校验位才能纠正一位错误？

### 练习 3：芯片组成

用 `32K×8 bit` 芯片组成 `128K×32 bit` 存储器，需要多少片？

### 练习 4：Cache

Cache 查询时间为 5 ns，未命中后额外访问主存需要 80 ns，命中率为 90%，平均访问时间是多少？

### 练习 5：流水线

某 4 段流水线各段均为 3 ns，执行 20 个任务需要多长时间？

### 练习 6：调度

三个进程同时到达，服务时间依次为 3、5、2。采用 FCFS，平均周转时间是多少？

### 练习 7：分页

页大小 2 KB，逻辑地址 5000 的页号和页内偏移分别是多少？

### 练习 8：网络

在 20 Mbps 链路上传送 5 MB 数据，忽略其他时延，发送时延是多少？按十进制单位计算。

### 练习 9：子网

`10.0.0.0/28` 每个普通子网传统可用主机数是多少？地址步长是多少？

### 练习 10：McCabe

某结构化程序有 6 个二分支判定节点，环路复杂度是多少？

### 练习 11：PERT

某活动三点估计为 3、6、15 天，求期望时间和方差。

### 练习 12：可靠性

3 个可靠度均为 0.8 的部件串联，系统可靠度是多少？若改为并联呢？

---

## 八、综合练习答案与解析

### 练习 1

\[
-2^{15}\sim2^{15}-1=-32768\sim32767
\]

### 练习 2

检验 \(2^r\ge32+r+1\)：\(r=5\) 时 `32<38`，不满足；\(r=6\) 时 `64≥39`，所以至少 6 位。

### 练习 3

字扩展 \(128K/32K=4\)，位扩展 \(32/8=4\)，共 \(4\times4=16\) 片。

### 练习 4

每次先查询 Cache；未命中时再额外访问主存：

\[
T_{avg}=5+(1-0.9)\times80=13\text{ ns}
\]

### 练习 5

每段时间相等：

\[
T=(4+20-1)\times3=69ns
\]

### 练习 6

FCFS 顺序不变。完成时间分别为 3、8、10，因同时到达，周转时间也是 3、8、10：

\[
平均周转时间=(3+8+10)/3=7
\]

### 练习 7

\(2KB=2048B\)：

\[
页号=\lfloor5000/2048\rfloor=2
\]

\[
偏移=5000-4096=904
\]

### 练习 8

\[
T=\frac{5\times10^6\times8}{20\times10^6}=2s
\]

### 练习 9

主机位为 \(32-28=4\)，传统可用主机数为 \(2^4-2=14\)，地址步长为 16。

### 练习 10

\[
V(G)=判定节点数+1=6+1=7
\]

### 练习 11

\[
t_e=(3+4\times6+15)/6=7\text{ 天}
\]

\[
\sigma^2=((15-3)/6)^2=4\text{ 天}^2
\]

### 练习 12

串联：

\[
R_s=0.8^3=0.512
\]

并联：

\[
R_p=1-(1-0.8)^3=1-0.008=0.992
\]

---

## 九、考前公式速查表

| 模块 | 公式 |
|---|---|
| 无符号数范围 | \(0\sim2^n-1\) |
| 补码范围 | \(-2^{n-1}\sim2^{n-1}-1\) |
| 海明校验位 | \(2^r\ge m+r+1\) |
| 地址线 | \(n\) 根地址线访问 \(2^n\) 个单元 |
| 芯片数 | 字扩展倍数 × 位扩展倍数 |
| Cache | 常见路径：\(T_{avg}=T_c+(1-h)T_m\)，\(T_m\) 为未命中后额外主存时间 |
| 流水线 | \(T=\sum t_i+(n-1)\Delta t\) |
| CPU 时间 | \(指令数\times CPI/主频\) |
| 磁盘平均旋转等待 | \(30/r\) 秒，\(r\) 为 rpm |
| 周转时间 | 完成时间 - 到达时间 |
| 带权周转时间 | 周转时间 / 服务时间 |
| 响应比 | \((等待+服务)/服务\) |
| 分页 | 页号=地址÷页大小，偏移=地址 mod 页大小 |
| 死锁资源下限 | \(n(k-1)+1\) |
| 发送时延 | 数据量(bit) / 速率(bit/s) |
| 传播时延 | 距离 / 传播速度 |
| 子网主机数 | 一般为 \(2^{32-p}-2\) |
| 奈奎斯特 | \(R=2W\log_2N\) |
| 香农 | \(C=W\log_2(1+S/N)\) |
| McCabe | \(V=E-N+2=判定节点数+1\) |
| PERT 期望 | \((a+4m+b)/6\) |
| PERT 方差 | \(((b-a)/6)^2\) |
| 总时差 | \(LS-ES=LF-EF\) |
| 串联可靠性 | \(\prod R_i\) |
| 并联可靠性 | \(1-\prod(1-R_i)\) |
| 沟通路径 | \(n(n-1)/2\) |
| 二叉树 | \(n_0=n_2+1\) |

---

## 十、复习优先级

### 第一优先级：必须熟练计算

1. 补码范围与转换。
2. 海明校验位。
3. 存储容量、地址范围、芯片组成。
4. Cache 平均访问时间。
5. 流水线执行时间、吞吐率、加速比。
6. 进程调度与分页地址转换。
7. 网络发送时延和子网划分。
8. McCabe、PERT、关键路径。
9. 常见算法复杂度。

### 第二优先级：掌握题型

1. CRC 模 2 除法。
2. 磁盘容量、旋转等待和调度。
3. 页面置换。
4. 系统可靠性。
5. 奈奎斯特、香农公式。
6. 哈希平均查找长度。

### 做题检查清单

1. 题目单位是否统一：`bit/Byte`、`KB/MB`、`MHz/GHz`、`ns/ms/s`。
2. 地址范围是否加 1。
3. 百分比是否变成小数。
4. 流水线第一项是否包含第一条任务通过全部阶段的时间。
5. Cache 是否先画了命中、未命中两条路径，并确认主存时间是额外代价还是整条未命中路径时间。
6. 调度算法是否抢占，进程是否同时到达。
7. 子网中全 0、全 1说的是主机号部分。
8. 关键路径是否取最长路径。
9. 方差是否平方。
10. 结果是否符合常识和数量级。
